<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://vgraman0.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://vgraman0.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-01-25T17:08:02+00:00</updated><id>https://vgraman0.github.io/feed.xml</id><title type="html">blank</title><subtitle>A personal technical website featuring blog posts, projects, book reviews, and repositories. </subtitle><entry><title type="html">3. Storage and Retrieval</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch3/" rel="alternate" type="text/html" title="3. Storage and Retrieval"/><published>2026-01-24T12:00:00+00:00</published><updated>2026-01-24T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch3</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch3/"><![CDATA[<p>This chapter is a foundational deep dive into how databases actually handle data under the hood.</p> <h1 id="database-data-structures">Database Data Structures</h1> <p>We can implement a simple key-value store with two Bash functions:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>db_set<span class="o">()</span> <span class="o">{</span>
    <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">,</span><span class="nv">$2</span><span class="s2">"</span> <span class="o">&gt;&gt;</span> database
<span class="o">}</span>

db_get<span class="o">()</span> <span class="o">{</span>
    <span class="nb">grep</span> <span class="s2">"^</span><span class="nv">$1</span><span class="s2">,"</span> database | <span class="nb">sed</span> <span class="nt">-e</span> <span class="s2">"s/^1,//"</span> | <span class="nb">tail</span> <span class="nt">-n</span> 1
<span class="o">}</span>
</code></pre></div></div> <p>The underlying storage format is a text file where each line contains a key-value pair (comma-separated).</p> <ul> <li><code class="language-plaintext highlighter-rouge">db_set()</code> appends a new key-value pair to a text file</li> <li><code class="language-plaintext highlighter-rouge">db_get()</code> uses grep to find the last occurrence of the key in the file to return the latest value.</li> </ul> <p>The file that we use for storage and retrieval is a <strong>log</strong>, an append-only data file.</p> <p>This simple data structure comes with the trade-offs:</p> <ul> <li><strong>Performant writes</strong>: appending to the file is very efficient because it is a sequential operation.</li> <li><strong>Poor read performance</strong>: The cost of a lookup is $O(n)$, where $n$ is the number of entries.</li> </ul> <p>The solve this, we need an <strong>index</strong>. This is additional metadata that helps to locate the data. This improves read performance but incurs overhead since we need to write additional structures. Because of this, choosing an index is left to the application developer, using the knowledge of the typical query patterns.</p> <h3 id="hash-index">Hash Index</h3> <p>The simplest indexing strategy is to keep an <strong>in-memory hash map</strong> where every key is mapped to a <strong>byte offset</strong> in the data file.</p> <ul> <li>When writing a key, update the hash map with the offset of the data appended.</li> <li>To read, look up the offset in the hash map, seek to that location on disk, and read that value.</li> <li><strong>Constraint</strong>: All keys must fit in available RAM. This makes it perfect for workloads with many writes per key (like a counter), but a low number of distinct keys. This approach is used for <strong>Bitcask</strong> (the default storage engine for Riak).</li> </ul> <h3 id="compaction">Compaction</h3> <p>If we only ever append to a file, we might eventually run out of disk space. The solution is <strong>compaction</strong>:</p> <ol> <li>Break the log into segments.</li> <li>A background process throws away duplicate keys, keeping only the most recent update for each.</li> <li>Segments are merged together to keep the total number of files small.</li> </ol> <h3 id="practical-issues">Practical issues</h3> <p>While our key-value store was simple, there are a number of practical issues that need to be addressed for real database systems.</p> <ul> <li><strong>File format</strong>: CSV is easy for humans to read, but is inefficient for a database. <ul> <li>Use a binary format encoding the length of a string first, followed by raw bytes.</li> <li>This is faster for a machine to parse and simpler to process.</li> </ul> </li> <li><strong>Deleting records</strong>: In an append-only log, we cannot erase old lines. <ul> <li>Append a special record called a <strong>tombstone</strong> associated with the key.</li> <li>During compaction, it sees the tombstone and knows to discard any previous values for that key, eventually removing it from the new, merged segments.</li> </ul> </li> <li><strong>Crash recovery</strong>: Because the hash map lives in memory, a crash would wipe the index. <ul> <li><em>Naive solution</em>: rebuild the index by reading all the segments. For large files, the makes restarts very slow.</li> <li><em>Bitcask optimization</em>: Store a snapshot of each segment’s has map on disk. When the system reboots, it can load the snapshots into memory, which is faster than a full scan.</li> </ul> </li> <li><strong>Partially written records</strong>: A carsh can happen while the database is writing a record. <ul> <li>Detect the partial entries using checksums that are included in the file.</li> <li>When detecting a corrupted record, it can safely ignore that partial entry.</li> </ul> </li> <li><strong>Concurrency</strong>: handling multiple users at once is a classic database challenge. <ul> <li><em>Single writer</em>: many implementations use a single writer thread to avoid ace conditions.</li> <li><em>Multiple readers</em>: The data segments are immutable, so many threads can lead simultaneously without locking.</li> </ul> </li> </ul> <h3 id="tradeoffs">Tradeoffs</h3> <p>It might seem wasteful to never “overwrite” data in place, but it offers many advantages:</p> <ol> <li><em>Sequential writes</em>: Appending is much faster than random-access writes, especially on hard disks.</li> <li><em>Simpler recovery</em>: These are much simpler if segment files are immutable.</li> <li><em>No fragmentation</em>: Periodically merging segments prevents the data files from getting fragmented over time.</li> </ol> <p>There are still some limitations of the hash table index:</p> <ol> <li>The hash table must fit into memory, so it will not work for a massive number of keys. <ul> <li>Theoretically, we could use an on-disk hash map. But this would perform very poorly since it requires significant random access I/O.</li> </ul> </li> <li>Range queries are not efficient. It requires looking up each individual key in the range.</li> </ol> <h2 id="log-sorted-merge-trees-lsm-trees">Log-Sorted Merge-Trees (LSM-trees)</h2> <p><strong>Sorted String Tables (SSTables)</strong> address the limitations of our original log-structured storage by requiring that the sequence of key-value pairs in our segment files are <em>sorted by key</em>.</p> <ul> <li><strong>Efficient merging</strong>: Even if the segment files are larger than the available memory, we can merge them using a technique similar to merge sort. <ul> <li>Read multiple segments side-by-side, look at the first key in each, and copy the lowest key to a new output file (similar to <strong>mergesort</strong>). This produces a new merged segment file that is sorted by key.</li> </ul> </li> <li><strong>Sparse index</strong>: The in-memory index can be <strong>sparse</strong> since it only needs to store the offset for the start of each segment.</li> <li><strong>Block compression</strong>: Since search requires scanning within the segment range, we can group records into <strong>compressed blocks</strong> before writing them to disk. <ul> <li>The sparse index points to the start of these compressed chunks.</li> </ul> </li> <li><strong>Efficient range queries</strong>: Since the keys are physically adjacent on disk, range queries become a simple <strong>sequential read</strong>.</li> </ul> <h3 id="constructing-and-maintaining-sstables">Constructing and maintaining SSTables</h3> <p>Our incoming writes can occur in any order, so we need an additional structure to maintain the sort.</p> <ol> <li>When a write arrives, it is added to an in-memory balanced tree structure (like a red-black tree), called a <strong>memtable</strong>. <ul> <li>Once the memtable grows past a certain size threshold (a few megabytes), it is written to disk as an SSTable file.</li> <li>While the old memtable is being written to disk, a new memtable instance handles incoming writes.</li> </ul> </li> <li>To find a key, the storage engine <strong>first checks the memtable</strong> to see if the key is in-memory. Otherwise, it <strong>checks the SSTable segments</strong> (starting with the most recent segment, then the next-older segment, etc).</li> <li>A compaction process runs in the background to combine segment files and to discard overwritten or deleted values.</li> </ol> <p>One scenario we still need to handle is the case where the database crashes. In this case, everything in the memtable is wiped out since it hasn’t been written to an SSTable yet. To resolve this, we use an <strong>append-only log</strong> corresponding to each memtable. - Each write is appended to an unsorted log before being added to the memtable. - If the system crashes, the database reads to log to rebuild the memtable. - Once a memtable is flushed to an SSTable, the recovery log is deleted because the data has already been persisted.</p> <p>This whole scheme—memtable + recovery log + SSTables + background merging—is a <strong>Log-Structured Merge-Tree (LSM-Tree)</strong>.</p> <h3 id="bloom-filters">Bloom Filters</h3> <p>The biggest performance bottleneck in an LSM-tree occurs when you search for a key that does not exist in the database.</p> <ul> <li>Because the data is spread across the memtable and multiple SSTable segments, the engine has to check the memtable, and then every segment on disk before it can detect that the key is missing.</li> <li>This leads to many unnecessary and expensive disk reads.</li> </ul> <p>A <strong>Bloom filter</strong> is a memory-efficient data structure used to approximate the contents of a set.</p> <ul> <li>When a key is added to the database, it is “hashed” several times, and the corresponding bits in a bit array are set to $1$.</li> <li>If we check the filter for a key and any of the bits are $0$, the engine knows that the key is not in the database.</li> <li>If all the bits are $1$, the key is there with some probability. There is a change of a false positive, but the engine can fall back to a standard disk scan in those cases.</li> </ul> <h4 id="false-positive-rate">False positive rate</h4> <p>Suppose we have a bit array of size $m$ and $k$ independent hash functions.</p> <p>The probability a specific bit is still $0$ after $k$ hashes for $n$ elements is</p> \[\left(1 - \frac{1}{m} \right)^{kn}.\] <p>Using the identity $\lim_{x \to \infty} \left(1 - \frac{1}{x} \right)^x = \frac{1}{e}$, we can approximate</p> \[\left(1 - \frac{1}{m} \right)^{kn} = \left(1 - \frac{1}{m} \right)^{m \cdot \frac{kn}{m}} \approx e^{-kn/m}.\] <p>Thus, the probability a bit is $1$ is given by</p> \[1 - e^{-kn/m}.\] <p>Finally, the false positive rate $\epsilon$ is the probability that all $k$ bits for a new element are already set to $1$. This is given by</p> \[\epsilon = \left( 1 - \left(1 - \frac{1}{m} \right)^{kn}\right)^k\approx \left(1 - e^{-kn/m}\right)^k.\] <h2 id="b-trees">B-trees</h2> <p>While LSM-trees are gaining popularity, <strong>B-trees</strong> are the most widely used indexing structure and the standard implementation in almost all relational databases.</p> <p>The core difference between the two has to do with how they organize data on disk:</p> <ul> <li>LSM-trees break the database into variable-size segments (several MBs) and write them sequentially.</li> <li>B-trees break the database into fixed-size blocks/pages (4 KB in size).</li> </ul> <p>A B-tree lookup starts at a single <strong>root page</strong>.</p> <ul> <li>Each page contains keys and <strong>references to child pages</strong>. Each child is responsible for a continuous range of keys.</li> <li>The number of references in one page is the <strong>branching factor</strong>. In practice, this is typically several hundred.</li> <li>Because of the high branching factor, a massive database can be navigated in a tree only three or four levels deep. <ul> <li>A 4-level tree of 4 KB pages can store up to <strong>256 TB</strong>.</li> </ul> </li> </ul> <p>To update the value for an existing key in a $B$-tree:</p> <ul> <li>Search for the leaf page containing the key, change the value, and write that page back to disk.</li> <li>If a new key is added and there isn’t enough space in a page, it is <strong>split</strong> into two half-full pages.</li> <li>The parent page is then updated to account for the new subdivision of key ranges. This ensures that the tree remains balanced with a depth of $O(\log n)$.</li> </ul> <p>Because B-trees overwrite pages, a crash during a page split is dangerous—it can leave the index corrupted (an “orphan” page with no parent).</p> <ul> <li>To make the database resilient, B-trees use a <strong>write-ahead log (WAL)</strong>.</li> <li>This is an append-only file where every modification is written before it is applied to the tree pages.</li> <li>The database can use this log to restore the tree to a consistent state.</li> </ul> <p>Since multiple threads might access the tree simultaneously, B-trees protect their data structures with <strong>latches</strong> to provent a thread from seeing the tree in an inconsistent state during a split.</p> <h3 id="b-tree-optimizations">B-tree optimizations</h3> <p>B-trees have been the industry standard since the 1970s and engineers have developed several optimizations for extra performance and reliability.</p> <p><strong>Copy-on-Write (Shadow Paging)</strong>: Instead of overwriting a page in-place and relying on a WAL, some databases (e.g. LMDB) use copy-on-write:</p> <ul> <li>A modified page is written to a new location on disk.</li> <li>The database then creates new versions of parent pages that point to this new location, cascading up the tree.</li> <li>This is helpful for <strong>snapshot isolation</strong>: readers can continue looking at the old version of the tree while a writer is busy creating a new one.</li> </ul> <p><strong>Key Compression (Abbreviation)</strong>: Increase the efficiency of each page by reducing the space each key takes up.</p> <ul> <li>Interior pages don’t need the full key. They only need enough information to act as a boundary between pages.</li> <li>By packing more keys onto a single page, we increase the branching factor and reduce the depth.</li> </ul> <p><strong>Sequential Layout</strong>: In a standard B-tree, pages can be scattered anywhere on disk. This is inefficient for range scans because every new page might require a random disk seek.</p> <ul> <li>Many implementations try to lay out the tree so that leaf pages appear in sequential order on disk.</li> <li>Maintaining this order is very difficult as the tree grows and pages split.</li> <li>By contrast, LSM-trees have an advantage since the merging process naturally rewrites data in large, sequential segments.</li> </ul> <p><strong>Sibling pointers</strong>: To make range scans faster, many B-trees add extra pointers that aren’t part of the strict tree hierarchy.</p> <ul> <li>Each leaf page can contain a reference to its sibling pages to the left and right.</li> <li>This allows a query to scan across the leaves in order without having to jump bank up to the parent pages for every new range.</li> </ul> <h3 id="comparing-b-trees-and-lsm-trees">Comparing B-Trees and LSM-Trees</h3> <p><strong>Write throughput</strong>:</p> <ul> <li>LSM-trees support high write throughput because they turn random writes into sequential writes on disk.</li> <li>B-trees are slower for writes because every piece of data must be written twice: once to the WAL and once to the tree page itself.</li> </ul> <p><strong>Write amplification</strong>: Overwriting blocks on SSDs will eventually cause the performance to degrade.</p> <ul> <li>B-trees must write an entire 4 KB page even if only a few bytes change</li> <li>LSM-trees rewrite data during background compaction.</li> </ul> <p><strong>Read performance</strong>:</p> <ul> <li>B-trees are faster for reads because each key exists in exactly one place in the index.</li> <li>LSM-trees are slower since they have to check the memtable and multiple SSTable segments at different stages of compaction.</li> </ul> <p><strong>Storage</strong>:</p> <ul> <li>LSM-trees are not page-oriented and periodically rewrite SSTables to remove fragmentation, resulting in lower storage overhead. <ul> <li>LSM-trees can also be compressed more effectively than B-trees.</li> </ul> </li> <li>B-trees leave some disk space unused (during a page split or when a row cannot fit onto a page), leading to fragmentation.</li> </ul> <h2 id="other-indexing-structures">Other indexing structures</h2> <p><strong>Secondary Indexes</strong>: Useful for performing joins and finding records by non-unique fields.</p> <ul> <li>While primary keys uniquely identify a row, a secondary index key may refer to many matching row identifiers.</li> <li>The non-uniqueness is handled by making each value a list of matching row IDs (posting list) or appending a row ID to make each index key unique.</li> </ul> <p><strong>Storing values within the index</strong>: While the index is the thing that queries search for, the value could be the actual row or a reference to the row.</p> <ul> <li><strong>Heap files</strong>: Index contains a pointer to a location in a heap file where the data lives.</li> <li><strong>Clustered index</strong>: Stores the indexed row directly within the index. <ul> <li>Faster reads because there is no extra hop from the index to a separate heap file.</li> <li>Higher write overhead and increased storage requirements due to the data being embedded in the index.</li> </ul> </li> <li><strong>Covering index</strong>: A middle ground that only stores some columns alongside the index key. <ul> <li>If the query asks for columns that are already in the index, the database can answer the query without looking at the main table.</li> </ul> </li> </ul> <p><strong>Multi-column indexes</strong>: Some queries require multiple fields simultaneously.</p> <ul> <li><strong>Concatenated index</strong>: Combines fields into a single key. This is useful for queries matching the prefix of the index but useless for searching by the second column alone.</li> <li><strong>Multi-dimentional indexes</strong>: Essential for geospatial data. A standard B-tree cannot efficiently handle 2D range queries. <ul> <li>Specialized spatial indexes such as R-trees.</li> </ul> </li> </ul> <h1 id="column-oriented-storage">Column-Oriented Storage</h1> <p>For analytic queries that only need 4 or 5 columns out of a 100-column wide table, a <strong>row-oriented</strong> engine must still load every row from disk into memory. The engine has to parse these wide rows and filter out the vast majority of attributes, which is a waste of I/O and time.</p> <p>Instead, <strong>column-oriented storage</strong> stores all values from each column together in separate files.</p> <ul> <li>A query only needs to read and parse specific columns required for that query.</li> <li>The database can reassemble a row $k$ by taking the $k$-th entry from each individual column file.</li> </ul> <h2 id="compression">Compression</h2> <p><strong>Bitmap encoding</strong>: when the number of distinct values in a column is small relative to the total number of rows, we can compress the values into a bitmap.</p> <p><img src="/assets/img/ddia/ch3-bitmap-encoding.png" alt="Bitmap encoding for column compression"/></p> <p>The database creates a separate bitmap for every distinct value in the column.</p> <ul> <li>Each bitmap has one bit for every row in the table.</li> <li>The bit is set to $1$ if the row contains the specific value, and $0$ otherwise.</li> </ul> <p>When the number of distinct values is very large, the bitmaps will be sparse. In that case, we can use <strong>run-length encoding</strong> which makes the representation of the binary value string more compact.</p>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><category term="storage"/><category term="retrieval"/><summary type="html"><![CDATA[Indexes, B-trees vs LSM-trees, Column-Oriented Storage]]></summary></entry><entry><title type="html">4. Interest Rates</title><link href="https://vgraman0.github.io/blog/2026/hull-options-ch4/" rel="alternate" type="text/html" title="4. Interest Rates"/><published>2026-01-20T12:00:00+00:00</published><updated>2026-01-20T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/hull-options-ch4</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/hull-options-ch4/"><![CDATA[<h2 id="types-of-rates">Types of Rates</h2> <p><strong>Treasury rates</strong>: Rates on T-bills and T-bonds, considered risk-free.</p> <p><strong>Overnight rates</strong>: Fed funds rate in the US (other countries have similar systems). Arises from reserve requirements—banks with excess reserves lend overnight to banks with shortfalls.</p> <p><strong>Repo rate</strong>: Interest rate on a repurchase agreement, where securities are sold with an agreement to buy them back later. Effectively a collateralized loan.</p> <h2 id="reference-rates">Reference Rates</h2> <p><strong>LIBOR</strong> (London Interbank Offered Rate): Banks submitted estimates of their borrowing rates. Phased out due to manipulation scandals and lack of underlying transactions.</p> <p><strong>SOFR</strong> (Secured Overnight Financing Rate): The replacement for USD LIBOR. A volume-weighted median of rates on overnight Treasury repo transactions—based on actual trades rather than estimates.</p> <h2 id="bond-pricing">Bond pricing</h2> <p>A bond is a series of future cash flows: periodic coupons and a final principal (face value).</p> <p>Suppose the coupon $c_i$ payments are at times $t_i$, $P$ is the principal, and $r_i$ is the zero-rate for maturity $t_i$. The formula for the bond price is given as</p> \[P_{\text{bond}} = \sum_{i=1}^n c_i e^{-r_i t_i} + Pe^{-r_nt_n}.\] <p>This comes from discounting the values of the future cash flows to their corresponding present value.</p> <h3 id="bond-yield">Bond yield</h3> <p>The yield is a single “average” interest rate such that after being applied to each of the bond cash flows, the present value would equal the market price.</p> <p>In other words, we would solve the equation</p> \[P_{\text{bond}}(y) = \sum_{i=1}^n c_i e^{-y t_i} + Pe^{-yt_n} = P_{\text{market}}\] <p>This is usually calculated through an iterative method, such as Newton-Raphson.</p> <h3 id="par-yield">Par yield</h3> <p>The par yield is the coupon rate (per annum) that causes the bond price to equal its principal.</p> <p>For a bond that pays semi-annual coupons, we would solve</p> \[P_{\text{bond}}(c) = \frac{c}{2}\sum_{i=1}^n e^{-y t_i} + Pe^{-yt_n} = P\] <p>Rearranging the equation gives</p> \[c = 2P\left (\frac{1 - e^{-yt_n}}{\sum_{i=1}^n e^{-yt_i}}\right)\] <p>This is usually written as</p> \[c = \frac{P(1-d)m}{A},\] <p>where $A$ is the value of the annuity that pays one dollar on each coupon payment date, $m$ is the number of coupon payments per year, and $d$ is the present value of one dollar received at the maturity of the bond.</p> <h3 id="sensitivity">Sensitivity</h3> <p>When interest rates change $y$, the corresponding bond price will also change.</p> <p>To model this, we first approximate the change in a bond’s price given a change in yield $\Delta y$ as</p> \[\Delta P \approx \frac{dP}{dy} \Delta y + \frac{1}{2} \frac{d^2 P}{dy^2} (\Delta y)^2.\] <p>Normalizing by the bond price, we obtain</p> \[\frac{\Delta P}{P} = -D \Delta y + \frac{C}{2}\left( \Delta y\right)^2\] <p>where</p> \[D = -\frac{1}{P} \frac{dP}{dy}\] <p>and</p> \[C = \frac{1}{P} \frac{d^2P}{dy^2}\] <p>are the duration and convexity respectively.</p> <h2 id="forward-rates">Forward rates</h2> <p>A <strong>Forward Rate</strong> is the interest rate for a future period of time that is implied by the interest rates we see in the market today.</p> <p>Suppose $R_1$ and $R_2$ are the zero rates for maturities $T_1$ and $T_2$ respectively (where $T_2$ &gt; $T_1$).</p> <p>The forward rate $R_F$ is the rate we need to invest between $T_1$ and $T_2$ so that investing in at $R_2$ for $T_2$ years is equivalent to investing at $R_1$ for $T_1$ years and investing at $R_F$ between $T_1$ and $T_2$.</p> <p>Written out explicitly,</p> \[\begin{align*} e^{R_2T_2} &amp;= e^{R_1T_1} \cdot e^{R_F(T_2 - T_1)} \\ &amp;= e^{R_1T_1 + R_F(T_2 - T_1)} \end{align*}\] <p>Because $e^x$ is a monotonic function, we can apply the inverse to both sides to obtain</p> \[R_2T_2 = R_1T_1 + R_F(T_2 - T_1).\] <p>Rearranging and solving for $R_F$ gives</p> \[R_F = \frac{R_2T_2 - R_1T_1}{T_2 - T_1}.\] <h3 id="instantaneous-rates">Instantaneous rates</h3> <p>The <strong>instantaneous forward rate</strong> $f(T)$ represents the interest rate at a specific instant in time, i.e. the percentage growth rate of the investment at time $T$.</p> <p>Suppose $V(T)$ is the value of one dollar at time $T$. Then, \(f(T) := \frac{V'(T)}{V(T)}.\)</p> <p>We know that $V(T) = e^{R(T)T}$, where $R(T)$ is the zero rate at time $T$.</p> \[\begin{align*} V'(T) &amp;= e^{R(T)T} \frac{d}{dT} \left[R(T) T\right] \\ &amp;=- V(T) \frac{d}{dT}\log V(T). \end{align*}\] <p>This gives the expression</p> \[f(T) = -\frac{\partial }{\partial t} \log V(t).\] <p>Using the fundamental theorem of calculus,</p> \[\int_0^T f(t)\, dt = -\int_0^T \frac{\partial}{\partial t} \log V(t)\,dt = - \left(\log V(T) - \log V(0)\right).\] <p>We know $V(0) = 1$, so</p> \[\int_0^T f(t)\,dt = - \log V(T) = R(T)T.\] <p>Rearranging the expression, we obtain</p> \[R(T) = \frac{1}{T} \int_0^T f(t)\,dt.\] <p>In other words, the zero rate is the time-weighted average of the instantaneous forward rates between today and maturity.</p>]]></content><author><name></name></author><category term="hull-options"/><category term="options"/><category term="derivatives"/><category term="interest-rates"/><summary type="html"><![CDATA[Technical overview of rate types (SOFR/LIBOR), bond pricing, yield sensitivity (Duration/Convexity) and forward rates.]]></summary></entry><entry><title type="html">2. Data Models and Query Languages</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch2/" rel="alternate" type="text/html" title="2. Data Models and Query Languages"/><published>2026-01-16T12:00:00+00:00</published><updated>2026-01-16T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch2</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch2/"><![CDATA[<p>Data models are perhaps the most important part of developing software. They shape not only how we write code, but how we <em>think</em> about the problem we’re solving. Most applications are built by layering data models: application objects map to database tables, which map to bytes in memory, which map to electrical currents. Each layer hides the complexity below it by providing a clean abstraction.</p> <h2 id="relational-model-vs-document-model">Relational Model vs Document Model</h2> <p>The <strong>relational model</strong> organizes data into relations (called tables in SQL), where each relation is an unordered collection of tuples (rows). Proposed by Edgar Codd in 1970, it became the dominant model for structured data:</p> <ul> <li><strong>Transaction processing</strong>: Banking, airline reservations, stock trading</li> <li><strong>Batch processing</strong>: Payroll, reporting, analytics</li> </ul> <p>Relational databases generalized well beyond their original business data processing use case, and remained dominant for decades.</p> <h3 id="the-rise-of-nosql">The Rise of NoSQL</h3> <p>NoSQL databases emerged in the 2010s with several driving forces:</p> <ul> <li><strong>Scalability</strong>: Better handling of very large datasets or high write throughput than relational databases could offer</li> <li><strong>Specialized queries</strong>: Support for operations not well-served by the relational model</li> <li><strong>Schema flexibility</strong>: A more dynamic and expressive data model than rigid SQL schemas</li> <li><strong>Open source</strong>: Preference for free software over commercial database products</li> </ul> <p>The name “NoSQL” is somewhat of a misnomer—it’s better understood as “Not Only SQL.” Many applications now use a <strong>polyglot persistence</strong> approach, choosing the best tool for each use case.</p> <h3 id="the-object-relational-mismatch">The Object-Relational Mismatch</h3> <p>Most application code today is written in object-oriented languages. When data lives in relational tables, there’s an awkward translation layer between objects and rows/columns.</p> <blockquote> <p><strong>Impedance mismatch</strong>: The disconnect between the object-oriented programming model and the relational data model.</p> </blockquote> <p>Object-relational mapping (ORM) frameworks like ActiveRecord and Hibernate reduce boilerplate, but can’t completely hide the differences.</p> <h3 id="one-to-many-relationships">One-to-Many Relationships</h3> <p>Consider a LinkedIn profile: one person has multiple jobs, multiple education entries, and multiple contact methods. In a relational database, this requires <strong>normalization</strong>—splitting the data across multiple tables linked by foreign keys:</p> <p><img src="/assets/img/ddia/ch2-linkedin-relational.png" alt="Representing a LinkedIn profile in a relational schema requires multiple tables"/></p> <p>Document databases like MongoDB handle this more naturally. A JSON document can represent the entire profile in one place:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"user_id"</span><span class="p">:</span><span class="w"> </span><span class="mi">251</span><span class="p">,</span><span class="w">
  </span><span class="nl">"first_name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Bill"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"last_name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Gates"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"positions"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"job_title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Co-chair"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"organization"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Bill &amp; Melinda Gates Foundation"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"job_title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Co-founder"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"organization"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Microsoft"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"education"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"school_name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Harvard University"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"start"</span><span class="p">:</span><span class="w"> </span><span class="mi">1973</span><span class="p">,</span><span class="w">
      </span><span class="nl">"end"</span><span class="p">:</span><span class="w"> </span><span class="mi">1975</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <blockquote> <p><strong>Data locality</strong>: Document models keep related information together, enabling retrieval with a single query rather than multiple joins.</p> </blockquote> <h3 id="many-to-one-and-many-to-many-relationships">Many-to-One and Many-to-Many Relationships</h3> <p>Document models excel at one-to-many relationships but struggle with many-to-one and many-to-many relationships. If multiple people work at the same organization, that organization should be stored once and referenced—but document databases don’t traditionally support joins.</p> <p>This isn’t a new tension. The <strong>hierarchical model</strong> (IBM’s IMS, 1960s) also represented data as nested records and faced the same limitation.</p> <p>The <strong>network model</strong> (CODASYL) generalized hierarchical data by allowing records to have multiple parents:</p> <ul> <li>Records were linked like pointers in a programming language</li> <li>Accessing data required traversing an <strong>access path</strong> from a root record</li> <li>Application developers had to manage these traversal algorithms manually, making the code fragile and difficult to change</li> </ul> <p>The relational model solved this by letting the query optimizer choose the access path automatically—a key reason for its success.</p> <h3 id="schema-flexibility">Schema Flexibility</h3> <blockquote> <p><strong>Schema-on-read</strong>: The structure of the data is implicit and only interpreted when data is read. (Document databases)</p> <p><strong>Schema-on-write</strong>: The schema is explicit and the database enforces that all data conforms to it. (Relational databases)</p> </blockquote> <p>Schema-on-read is similar to dynamic typing in programming languages; schema-on-write is like static typing. Neither is universally better—the choice depends on the application.</p> <p>Schema-on-read is advantageous when:</p> <ul> <li>Items in a collection don’t all have the same structure</li> <li>The structure is determined by external systems you don’t control</li> <li>The schema changes frequently</li> </ul> <h3 id="convergence">Convergence</h3> <p>The distinction between relational and document databases is blurring:</p> <ul> <li>Most relational databases now support JSON documents and XML</li> <li>Some document databases support relational-like joins (RethinkDB)</li> <li>MongoDB drivers can automatically resolve document references (client-side joins)</li> </ul> <h2 id="graph-like-data-models">Graph-Like Data Models</h2> <p>When many-to-many relationships are common in your data, the graph model becomes natural. Graphs consist of <strong>vertices</strong> (nodes) and <strong>edges</strong> (relationships), and can model diverse data elegantly:</p> <p><img src="/assets/img/ddia/ch2-graph-model.png" alt="Graph data model representing people and locations"/></p> <p>Graphs aren’t limited to homogeneous data. Facebook’s social graph includes people, locations, events, checkins, and comments—all as vertices with different types of edges connecting them.</p> <h3 id="property-graphs">Property Graphs</h3> <p>In the <strong>property graph</strong> model (used by Neo4j, Titan, InfiniteGraph), each vertex has:</p> <ul> <li>A unique identifier</li> <li>Outgoing edges</li> <li>Incoming edges</li> <li>Properties (key-value pairs)</li> </ul> <p>Each edge has:</p> <ul> <li>A unique identifier</li> <li>Tail vertex (where the edge starts)</li> <li>Head vertex (where the edge ends)</li> <li>A label describing the relationship</li> <li>Properties (key-value pairs)</li> </ul> <p>You could model this in a relational database with two tables (vertices and edges), but graph queries become unwieldy. Finding all vertices within two hops requires two joins; within three hops requires three joins. Since the number of joins isn’t known in advance, you’d need recursive SQL queries.</p> <p><strong>Cypher</strong> is a declarative query language designed for property graphs (created for Neo4j). Finding everyone who emigrated from the US to Europe:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MATCH
(person) -[:BORN_IN]-&gt;  () -[:WITHIN*0..]-&gt; (us:Location {name:'United States'}),
(person) -[:LIVES_IN]-&gt; () -[:WITHIN*0..]-&gt; (eu:Location {name:'Europe'})
RETURN person.name
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">WITHIN*0..</code> syntax elegantly handles variable-length paths through the location hierarchy.</p> <h3 id="triple-stores">Triple-Stores</h3> <p>The <strong>triple-store</strong> model represents all information as three-part statements: <em>(subject, predicate, object)</em>.</p> <ul> <li>The <strong>subject</strong> is equivalent to a vertex</li> <li>The <strong>predicate</strong> and <strong>object</strong> are either: <ul> <li>A property: <code class="language-plaintext highlighter-rouge">(lucy, age, 33)</code> — Lucy is 33 years old</li> <li>A relationship: <code class="language-plaintext highlighter-rouge">(lucy, marriedTo, alain)</code> — Lucy is married to Alain</li> </ul> </li> </ul> <p>Triple-stores are closely related to the <strong>semantic web</strong> vision of machine-readable data on the internet, though that vision hasn’t fully materialized.</p> <p><strong>SPARQL</strong> is the query language for triple-stores, with syntax inspired by Cypher (or perhaps it’s the other way around—SPARQL predates Cypher).</p>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><category term="data"/><category term="query"/><summary type="html"><![CDATA[Comparing relational, document, and graph data models—and the query languages that accompany them]]></summary></entry><entry><title type="html">1. Reliable, Scalable, and Maintainable Applications</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch1/" rel="alternate" type="text/html" title="1. Reliable, Scalable, and Maintainable Applications"/><published>2026-01-15T12:00:00+00:00</published><updated>2026-01-15T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch1</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch1/"><![CDATA[<p>Many applications today are <strong>data-intensive</strong> rather than compute-intensive. The bottleneck is usually the amount of data, the complexity of data, and how fast it changes—not raw CPU power. This chapter establishes three fundamental concerns for building data systems.</p> <h2 id="thinking-about-data-systems">Thinking About Data Systems</h2> <p>Databases, caches, and queues are traditionally thought of as different categories of tools with different access patterns, performance characteristics, and implementations. But the boundaries are blurring:</p> <ul> <li><strong>Redis</strong>: A datastore that can also be used as a message queue</li> <li><strong>Kafka</strong>: A message queue with database-like durability guarantees</li> </ul> <p>Modern applications are usually too complex to solve with a single tool, requiring composite systems. In this view, the application developer becomes a <strong>data system designer</strong>, responsible for a system that provides specific guarantees to the client.</p> <p>The book focuses on three main concerns:</p> <ol> <li><strong>Reliability</strong>: Continuing to work correctly when things go wrong</li> <li><strong>Scalability</strong>: Having strategies to handle growth in load</li> <li><strong>Maintainability</strong>: Making it easy for people to work on the system over time</li> </ol> <h2 id="reliability">Reliability</h2> <p>First, distinguish between faults and failures:</p> <blockquote> <p><strong>Fault</strong>: When one component of the system deviates from its specification.</p> <p><strong>Failure</strong>: When the system as a whole stops providing the required service to the user.</p> </blockquote> <p>It’s impossible to reduce the probability of a fault to zero. The goal is to design <strong>fault-tolerant</strong> mechanisms that prevent faults from causing failures.</p> <p><strong>Types of faults:</strong></p> <ol> <li><strong>Hardware faults</strong>: Hard disks crashing, RAM failing, power outages. Usually handled with redundancy (RAID, backup generators).</li> <li><strong>Software errors</strong>: Systematic bugs in the system. Hard to anticipate and tend to cause more failures since they are correlated across nodes.</li> <li><strong>Human error</strong>: Configuration mistakes by operators—the leading cause of outages. <ul> <li>Design systems that minimize opportunities for error</li> <li>Provide sandbox environments to explore and experiment safely</li> </ul> </li> </ol> <h3 id="example-netflix-chaos-monkey">Example: Netflix Chaos Monkey</h3> <p>Netflix’s Chaos Monkey deliberately kills random server processes in production. The goal is to ensure that fault-tolerance mechanisms are actually working—not just theoretically sound.</p> <h2 id="scalability">Scalability</h2> <p>Scalability isn’t a simple “yes” or “no” label. It’s about how the system handles growth in specific circumstances.</p> <h3 id="describing-load">Describing Load</h3> <p>Choose <strong>load parameters</strong>—the dimensions of growth you want your system to handle:</p> <ul> <li>Requests per second</li> <li>Ratio of reads to writes</li> <li>Number of simultaneous users</li> </ul> <p><strong>Twitter case study:</strong></p> <ul> <li>Post tweet: avg 4.6k rps, peak 12k rps</li> <li>View timeline: 300k rps</li> </ul> <p>Handling 12k writes/sec is easy. But 300k reads/sec is harder—and Twitter guarantees tweet delivery to followers within 5 seconds.</p> <p><strong>Approach 1: Read-time joins</strong> Store tweets in a global table. On timeline request, look up everyone you follow, grab their tweets, and merge them. This is slow at 300k reads/sec.</p> <p><strong>Approach 2: Write-time fan-out</strong> Maintain a timeline cache per user. When someone tweets, insert it into every follower’s cache. Reading is fast since the work is pre-computed. But posting is expensive: with 75 followers on average, 4.6k tweets/sec becomes 345k cache writes/sec. This doesn’t scale for celebrities with millions of followers.</p> <p><strong>Twitter’s hybrid solution:</strong> Most users use write fan-out, but celebrity tweets are fetched separately and merged at read time.</p> <h3 id="measuring-performance">Measuring Performance</h3> <p>Different systems have different metrics:</p> <ul> <li><strong>Batch systems</strong> (Hadoop): Throughput (records/sec) or total processing time</li> <li><strong>Online systems</strong>: Response time—measured as a distribution, not a single number</li> </ul> <blockquote> <p><strong>Response time</strong> = time between client sending request and receiving response</p> <p><strong>Latency</strong> = time a request spends waiting to be handled</p> </blockquote> <p><strong>Percentiles matter more than averages.</strong> Amazon optimizes for p99.9 because customers with the most data (from many purchases) tend to have the slowest requests—and they’re the most valuable.</p> <p><strong>Tail latency amplification</strong>: In systems with multiple backend calls, a single slow sub-request can slow down the entire user response.</p> <h3 id="approaches-to-handle-load">Approaches to Handle Load</h3> <ol> <li><strong>Vertical scaling</strong>: Upgrade to a more powerful machine</li> <li><strong>Horizontal scaling</strong>: Distribute load across multiple machines <ul> <li><em>Elastic</em>: Automatically add resources when load increases</li> <li>Complex for stateful systems</li> </ul> </li> </ol> <p>Rule of thumb: Scale databases vertically until cost or availability requirements force distribution</p> <h2 id="maintainability">Maintainability</h2> <p>The majority of software cost is in ongoing maintenance, not initial development. Design for these three principles:</p> <ol> <li><strong>Operability</strong>: Make it easy for operations teams to keep the system running smoothly</li> <li><strong>Simplicity</strong>: Make it easy for new engineers to understand the system (reduce accidental complexity)</li> <li><strong>Evolvability</strong>: Make it easy to adapt the system for unanticipated use cases in the future</li> </ol>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><category term="reliability"/><category term="scalability"/><summary type="html"><![CDATA[The three fundamental concerns for building data systems: reliability, scalability, and maintainability]]></summary></entry><entry><title type="html">3. Hedging Strategies Using Futures</title><link href="https://vgraman0.github.io/blog/2026/hull-options-ch3/" rel="alternate" type="text/html" title="3. Hedging Strategies Using Futures"/><published>2026-01-11T12:00:00+00:00</published><updated>2026-01-11T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/hull-options-ch3</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/hull-options-ch3/"><![CDATA[<p>This chapter focuses on the practical application of futures: protecting against price movements. While the theory suggests a perfect “lock” on prices, real-world constraints like basis risk and asset mismatches make hedging an exercise in variance minimization.</p> <h3 id="short-vs-long-hedges">Short vs. Long Hedges</h3> <ul> <li><strong>Short Hedge:</strong> Occurs when you already own an asset (or will produce it, like a farmer with crops) and intend to sell it later. You sell futures at price $F$ to protect against a <strong>price decrease</strong>. <ul> <li>Sale of physical asset at time $T$: $+S_T$</li> <li>Payoff from Short Future: $+(F - S_T)$</li> <li><strong>Net Price Received:</strong> $S_T + F - S_T = F$</li> <li><strong>Result:</strong> If prices fall, the gain on the futures offsets the loss on the physical sale.</li> </ul> </li> <li><strong>Long Hedge:</strong> Occurs when you know you must buy an asset in the future (like an airline needing fuel). You buy futures at price $F$ to protect against a <strong>price increase</strong>. <ul> <li>Purchase of physical asset at time $T$: $-S_T$</li> <li>Payoff from Long Future: $+(S_T - F)$</li> <li><strong>Net Price Paid:</strong> $-S_T + S_T - F = -F$</li> <li><strong>Result:</strong> If prices rise, the gain on the futures offsets the higher cost of the physical purchase.</li> </ul> </li> </ul> <hr/> <h3 id="basis-risk">Basis Risk</h3> <p>In a perfect world, the spot price ($S$) and futures price ($F$) converge at expiry. In reality, we often close a hedge before expiry or at a different location. The <strong>Basis</strong> is defined as:</p> \[\text{Basis} = \text{Spot Price} - \text{Futures Price}\] <ul> <li><strong>Strengthening:</strong> If the Basis increases ($S - F \uparrow$), it benefits a short hedger but hurts a long hedger.</li> <li><strong>Weakening:</strong> If the Basis decreases ($S - F \downarrow$), it benefits a long hedger but hurts a short hedger.</li> </ul> <hr/> <h3 id="cross-hedging--minimum-variance-hedge-ratio">Cross Hedging &amp; Minimum Variance Hedge Ratio</h3> <p>When the asset being hedged is not the same as the underlying of the futures contract, we face <strong>cross hedging risk</strong>. We must find the optimal hedge ratio ($h^*$) that minimizes the variance of the hedged portfolio.</p> <h4 id="derivation">Derivation</h4> <p>The change in the value of a hedged position is $\Delta P = \Delta S - h \Delta F$. To minimize risk, we minimize the variance:</p> \[\begin{aligned} \operatorname{Var}(\Delta P) &amp;= \operatorname{Var}(\Delta S) + \operatorname{Var}(h\Delta F) - 2 \operatorname{Cov}(\Delta S, h\Delta F) \\ &amp;= \sigma_S^2 + h^2 \sigma_F^2 - 2h \rho \sigma_S \sigma_F \end{aligned}\] <p>Taking the derivative with respect to $h$ and setting it to zero:</p> \[\frac{d}{dh} \operatorname{Var}(\Delta P) = 2h \sigma_F^2 - 2 \rho \sigma_S \sigma_F = 0\] <p>Solving for the <strong>Minimum Variance Hedge Ratio ($h^*$)</strong>:</p> \[h^* = \rho \frac{\sigma_S}{\sigma_F} = \frac{\operatorname{Cov}(\Delta S, \Delta F)}{\sigma_F^2}\] <hr/> <h3 id="optimal-number-of-contracts-n">Optimal Number of Contracts ($N^*$)</h3> <p>Once $h^*$ is known, we calculate the number of contracts needed by adjusting for the size of the position ($Q_A$) and the size of one contract ($Q_F$):</p> \[N^* = \frac{h^* Q_A}{Q_F}\] <p><strong>Tailing the Hedge:</strong> Because futures are settled daily, we usually “tail” the hedge to account for the interest earned on margin cash flows. We use the total dollar value ($V$):</p> \[N^* = \hat{h} \frac{V_A}{V_F}\] <hr/> <h3 id="hedging-an-equity-portfolio">Hedging an Equity Portfolio</h3> <p>To hedge a diversified stock portfolio, we use stock index futures. Instead of calculating a custom $h^*$, we use the portfolio’s <strong>Beta ($\beta$)</strong>, which measures its sensitivity to the market index:</p> \[N^* = \beta \frac{V_A}{V_F}\] <ul> <li>If $\beta &gt; 1$, the portfolio is more volatile than the market, requiring more futures contracts for protection.</li> <li>If $\beta &lt; 1$, fewer contracts are required.</li> </ul>]]></content><author><name></name></author><category term="hull-options"/><category term="options"/><category term="derivatives"/><category term="hedging"/><summary type="html"><![CDATA[Using futures to hedge price risk, basis risk, and deriving the minimum variance hedge ratio]]></summary></entry><entry><title type="html">From Chicago to Mumbai: A Year of Shifting Gears and Building Foundations</title><link href="https://vgraman0.github.io/blog/2026/welcome/" rel="alternate" type="text/html" title="From Chicago to Mumbai: A Year of Shifting Gears and Building Foundations"/><published>2026-01-10T12:00:00+00:00</published><updated>2026-01-10T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/welcome</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/welcome/"><![CDATA[<p>As I look back on 2025, the word that comes to mind is <strong>agency</strong>. It was the year I stopped waiting for growth to happen to me and started engineering it myself. From managing my first team of interns in Chicago to relocating across the world to Mumbai, the last twelve months have been a masterclass in navigating ambiguity and technical ownership.</p> <h2 id="the-chicago-chapter-high-output-low-depth">The Chicago Chapter: High Output, Low Depth</h2> <p>The first half of 2025 at IMC was a study in efficiency versus evolution. Working across the valuation stack, I found myself in a comfortable rhythm: I understood the underlying mathematical models and could translate them into clean, functional features with speed.</p> <p>On paper, I was performing at a high level, but internally, I felt a plateau. I realized that much of my work consisted of BAU (Business As Usual) tasks—the kind of logical translation that, while necessary, lacked the architectural depth I craved. It was a sobering realization: these were precisely the types of tasks I expect AI to automate in the near future. I was mastering a skill set that was becoming a commodity, and I knew I needed to find a more complex “frontier” to stay ahead.</p> <h2 id="a-pivot-to-leadership-mentoring-interns">A Pivot to Leadership: Mentoring Interns</h2> <p>While my technical growth felt like it was hitting a ceiling, a new avenue for development opened up: management. I stepped into a lead role for two interns, a full-circle moment given that I had started my own journey in that very same program. This transition from “individual contributor” to “technical guide” provided the spark I was missing.</p> <p>Seeing my interns evolve from their first day to a successful project conclusion was incredibly rewarding, but more importantly, it was my first real calibration of leadership. I learned that guiding a team toward a technical goal requires a completely different “compiler”—one built on communication, project scoping, and the ability to unblock others rather than just writing the code myself.</p> <h2 id="moving-to-mumbai">Moving to Mumbai</h2> <p>Mid-year, I made a big move—literally. I asked for the opportunity to work out of our <strong>Mumbai office</strong>, and I’m grateful the company backed me on it.</p> <p>The environment here is a stark contrast to the US. The office is smaller and more “chaotic” in the best way possible. Without a massive density of local experts to lean on, I’ve had to lean on myself. This shift has forced two major changes in my workflow:</p> <ol> <li><strong>Project Management as a Survival Skill:</strong> My first project here spanned several teams. Navigating a smaller office meant building rapport from scratch and learning to structure communication more rigorously to keep things moving.</li> <li><strong>Active vs. Passive Learning:</strong> In Chicago, I realized that technical growth doesn’t just “happen” through osmosis. In Mumbai, I’ve used my free time to get back into “student mode.” But this time, it’s different.</li> </ol> <h2 id="moving-from-learning-to-building">Moving from “Learning” to “Building”</h2> <p>The biggest shift in the latter half of 2025 was breaking a lifelong habit of passive learning. Historically, my approach to growth was rooted in high-volume consumption. I used to pride myself on the “grind”—at one point during my time at UC Berkeley, I took ten math courses in a single semester. I was very good at grinding through homework assignments and taking exams, but I was collecting knowledge without ever “compiling” it. There is a big difference between taking a course and engaging deeply with a subject.</p> <p>The biggest shift in the latter half of 2025 was moving away from passive consumption. When I started the <strong>CS231n</strong> course and Karpathy’s <strong>makemore</strong>, I didn’t just watch the videos. I built the models and testing frameworks from scratch.</p> <p>I’ve realized that I learn best when I’m breaking things. To date, I’ve built <strong>three project prototypes</strong> in the last few months alone. Using AI as a force multiplier rather than a crutch has allowed me to move faster, but the core architectural decisions remain mine.</p> <hr/> <h1 id="looking-ahead-my-2026-north-stars">Looking Ahead: My 2026 North Stars</h1> <p>If 2025 was about realizing where the gaps were, 2026 is about closing them with precision. I’m focusing on three pillars:</p> <h3 id="1-the-deep-dive-cadence">1. The “Deep Dive” Cadence</h3> <p>I am committing to <strong>learning one new technical topic every month.</strong> Whether it’s a technical book or a specialized course, I’m going deep. To ensure the knowledge sticks, I’ll be writing a technical blog post for each one to summarize the core mechanics.</p> <h3 id="2-from-prototype-to-market">2. From Prototype to Market</h3> <p>Building in a vacuum is great for learning, but building for users is where the real challenge lies. In 2026, I want to take at least one of my project ideas and <strong>bring it to market.</strong> Learning the “product” side—user feedback, deployment, and scaling—is the next logical step in my evolution as an engineer.</p> <h3 id="3-mastering-the-art-of-communication">3. Mastering the Art of Communication</h3> <p>Technical skills are the floor; communication is the ceiling. I have two specific goals here:</p> <ul> <li><strong>Socially:</strong> As a natural introvert, I’m pushing myself out of my comfort zone. I’m making it a point to join local meetup groups and hackathons in Mumbai to build my social “muscle memory” and mingle with a broader technical community.</li> <li><strong>Professionally:</strong> My primary goal is to communicate complex logic and system designs with the same clarity and precision with which I write code. Often, there is a “lossy” translation when explaining valuation stacks or architectural trade-offs to non-technical stakeholders. I want my verbal explanations to have the same “readability” as a well-documented repository. To help bridge this gap, I’m starting a course from Vinh Giang to learn how to structure my thoughts. Learning how to communicate effectively is a lifelong journey and these are the first steps towards improving actively.</li> </ul> <hr/> <p>2025 was the year of the move. 2026 will be the year of the build. Let’s get to work.</p> <p>Here’s to a year of learning, building, and growing! 🚀</p>]]></content><author><name></name></author><category term="personal"/><category term="reflection"/><category term="goals"/><category term="career"/><summary type="html"><![CDATA[2025 reflection + 2026 broad goals]]></summary></entry></feed>