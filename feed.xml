<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://vgraman0.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://vgraman0.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-07T12:50:47+00:00</updated><id>https://vgraman0.github.io/feed.xml</id><title type="html">blank</title><subtitle>A personal technical website featuring blog posts, projects, book reviews, and repositories. </subtitle><entry><title type="html">MongoDB Deep Dive</title><link href="https://vgraman0.github.io/blog/2026/mongodb/" rel="alternate" type="text/html" title="MongoDB Deep Dive"/><published>2026-03-03T00:00:00+00:00</published><updated>2026-03-03T00:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/mongodb</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/mongodb/"><![CDATA[<p>This is an overview of MongoDB using an “interview-style” approach, that I’ve been using for learning. I’ve found that it is a useful way to think more actively about the subject and test the boundaries of my knowledge.</p> <p>This questions start with fundamentals (NoSQL, document format) all the way to more advanced distributed database concepts.</p> <h2 id="q1---what-is-mongodb-and-how-does-it-differ-from-a-traditional-relational-database-must-know">Q1 - What is MongoDB, and how does it differ from a traditional relational database? <span class="badge-difficulty badge-must-know">must-know</span></h2> <p>MongoDB is a NoSQL document database. Instead of storing data in rows and columns within fixed-schema tables, it stores flexible JSON-like documents (internally BSON) inside collections.</p> <p align="center"> <img src="/assets/2026-03-07-15-01-11.png" width="650"/> </p> <table> <thead> <tr> <th>Dimension</th> <th>Relational (Postgres/MySQL)</th> <th>MongoDB</th> </tr> </thead> <tbody> <tr> <td><strong>Schema</strong></td> <td>Rigid, enforced at write time</td> <td>Flexible; documents in the same collection can differ</td> </tr> <tr> <td><strong>Data model</strong></td> <td>Normalize across tables, JOIN at query time</td> <td>Embed related data in a single document</td> </tr> <tr> <td><strong>Query language</strong></td> <td>SQL</td> <td>Method/operator API (<code class="language-plaintext highlighter-rouge">find</code>, <code class="language-plaintext highlighter-rouge">$gt</code>, <code class="language-plaintext highlighter-rouge">$match</code>)</td> </tr> <tr> <td><strong>Scaling</strong></td> <td>Vertical by default; sharding via external tools</td> <td>Built-in horizontal sharding</td> </tr> <tr> <td><strong>Transactions</strong></td> <td>Mature multi-row ACID</td> <td>Multi-document ACID since v4.0, but single-doc operations are preferred</td> </tr> </tbody> </table> <p><strong>MongoDB</strong> shines when data is naturally document-shaped (product catalogs, CMS, user profiles), the schema evolves frequently, you need horizontal write scaling, or reads are document-centric. <strong>Relational</strong> is the better fit when data has complex many-to-many relationships, you need cross-entity queries with JOINs, or strong transactional integrity is non-negotiable.</p> <p>One reason MongoDB scales horizontally more easily than Postgres is architecture. Postgres is single-node by default. Replication gives read scaling, but writes go to a single primary and sharding requires external tools like Citus. MongoDB has built-in shard key mechanisms, config servers, and a <code class="language-plaintext highlighter-rouge">mongos</code> query router that handles distribution transparently.</p> <p>MongoDB also fills a different niche than blob storage like S3. S3 is a file store where you put/get objects by key. MongoDB lets you <strong>query inside</strong> the data, create indexes, run aggregation pipelines, do partial updates, and provides low-latency reads with transactional semantics. The 16 MB document limit rules out large binary objects anyway. Many systems use both: MongoDB for hot, queryable data and S3 for raw archives and large assets.</p> <hr/> <h2 id="q2---what-is-a-document-in-mongodb-and-what-format-does-it-use-must-know">Q2 - What is a document in MongoDB, and what format does it use? <span class="badge-difficulty badge-must-know">must-know</span></h2> <p>A document is the basic unit of data. It is analogous to a row in a relational table, but far more flexible. Documents are stored in <strong>BSON</strong> (Binary JSON).</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="dl">"</span><span class="s2">_id</span><span class="dl">"</span><span class="p">:</span> <span class="nc">ObjectId</span><span class="p">(</span><span class="dl">"</span><span class="s2">507f1f77bcf86cd799439011</span><span class="dl">"</span><span class="p">),</span>
  <span class="dl">"</span><span class="s2">name</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">Vishal</span><span class="dl">"</span><span class="p">,</span>
  <span class="dl">"</span><span class="s2">roles</span><span class="dl">"</span><span class="p">:</span> <span class="p">[</span><span class="dl">"</span><span class="s2">engineer</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">mentor</span><span class="dl">"</span><span class="p">],</span>
  <span class="dl">"</span><span class="s2">team</span><span class="dl">"</span><span class="p">:</span> <span class="p">{</span>
    <span class="dl">"</span><span class="s2">name</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">Strategy</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">system</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">IMC</span><span class="dl">"</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>Values can be strings, numbers, booleans, arrays, nested documents, dates, ObjectIds, binary data, Decimal128, and more.</p> <h3 id="why-bson-over-json">Why BSON over JSON?</h3> <ul> <li><strong>Richer types</strong>: <code class="language-plaintext highlighter-rouge">Date</code>, <code class="language-plaintext highlighter-rouge">ObjectId</code>, <code class="language-plaintext highlighter-rouge">Decimal128</code>, <code class="language-plaintext highlighter-rouge">int32</code> vs <code class="language-plaintext highlighter-rouge">int64</code> (none of which plain JSON supports)</li> <li><strong>Fast traversal</strong>: Length-prefixed fields let MongoDB skip to the field it needs without parsing the entire document</li> <li><strong>Compact encoding</strong>: BSON is more space-efficient than plain text JSON, which is important since MongoDB enforces a <strong>16 MB size limit</strong> per document</li> </ul> <p>Documents live inside <strong>collections</strong>, which are roughly analogous to tables but enforce no schema by default.</p> <hr/> <h2 id="q3---what-does-the-_id-field-do-and-what-happens-if-you-dont-provide-one-must-know">Q3 - What does the <code class="language-plaintext highlighter-rouge">_id</code> field do, and what happens if you don’t provide one? <span class="badge-difficulty badge-must-know">must-know</span></h2> <p><code class="language-plaintext highlighter-rouge">_id</code> is a unique identifier for every document (primary key). No two documents in the same collection can share the same <code class="language-plaintext highlighter-rouge">_id</code>. MongoDB automatically creates a unique index on it.</p> <p>If you don’t provide one, MongoDB generates an <strong>ObjectId</strong>: a 12-byte value with built-in structure:</p> <p align="center"> <img src="/assets/2026-03-07-15-01-29.png" width="650"/> </p> <ul> <li>The <strong>timestamp</strong> component means ObjectIds are roughly sortable by creation time</li> <li>You can extract the creation timestamp with <code class="language-plaintext highlighter-rouge">ObjectId.getTimestamp()</code></li> <li>You <em>can</em> provide your own <code class="language-plaintext highlighter-rouge">_id</code> (string, number, etc.), but you’re responsible for uniqueness</li> </ul> <hr/> <h2 id="q4---what-is-the-difference-between-find-and-findone-easy">Q4 - What is the difference between <code class="language-plaintext highlighter-rouge">find()</code> and <code class="language-plaintext highlighter-rouge">findOne()</code>? <span class="badge-difficulty badge-easy">easy</span></h2> <ul> <li><strong><code class="language-plaintext highlighter-rouge">find()</code></strong> returns a <strong>cursor</strong>, a lazy iterator that serves documents in batches. You chain <code class="language-plaintext highlighter-rouge">.sort()</code>, <code class="language-plaintext highlighter-rouge">.limit()</code>, <code class="language-plaintext highlighter-rouge">.skip()</code> onto it.</li> <li><strong><code class="language-plaintext highlighter-rouge">findOne()</code></strong> returns a single document directly, not a cursor. Effectively <code class="language-plaintext highlighter-rouge">find().limit(1)</code>.</li> <li><code class="language-plaintext highlighter-rouge">findOne()</code> is <strong>deterministic</strong>, not random. It returns the first match based on natural order or the index being used.</li> </ul> <p align="center"> <img src="/assets/2026-03-07-16-55-28.png" width="850"/> </p> <p><strong>When to use which</strong>: <code class="language-plaintext highlighter-rouge">findOne()</code> for lookups by <code class="language-plaintext highlighter-rouge">_id</code> or a unique field. <code class="language-plaintext highlighter-rouge">find()</code> any time you expect or want multiple results.</p> <p>The cursor returned by <code class="language-plaintext highlighter-rouge">find()</code> works on two levels. <strong>Server-side</strong>, MongoDB holds a reference to the result set, serving 101 documents in the first batch, then 16 MB batches. It has a <strong>10-minute inactivity timeout</strong>.</p> <p><strong>Client-side (Java)</strong>, the driver returns a <code class="language-plaintext highlighter-rouge">FindIterable</code> / <code class="language-plaintext highlighter-rouge">MongoCursor</code>. The query doesn’t execute until you start iterating (lazy evaluation). Close the cursor to avoid server-side leaks:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">FindIterable</span><span class="o">&lt;</span><span class="nc">Document</span><span class="o">&gt;</span> <span class="n">results</span> <span class="o">=</span> <span class="n">collection</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="n">eq</span><span class="o">(</span><span class="s">"status"</span><span class="o">,</span> <span class="s">"active"</span><span class="o">));</span>
<span class="k">try</span> <span class="o">(</span><span class="nc">MongoCursor</span><span class="o">&lt;</span><span class="nc">Document</span><span class="o">&gt;</span> <span class="n">cursor</span> <span class="o">=</span> <span class="n">results</span><span class="o">.</span><span class="na">iterator</span><span class="o">())</span> <span class="o">{</span>
    <span class="k">while</span> <span class="o">(</span><span class="n">cursor</span><span class="o">.</span><span class="na">hasNext</span><span class="o">())</span> <span class="o">{</span>
        <span class="nc">Document</span> <span class="n">doc</span> <span class="o">=</span> <span class="n">cursor</span><span class="o">.</span><span class="na">next</span><span class="o">();</span>
        <span class="c1">// process</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="q5---how-do-you-create-an-index-and-why-would-you-want-to-easy">Q5 - How do you create an index, and why would you want to? <span class="badge-difficulty badge-easy">easy</span></h2> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">db</span><span class="p">.</span><span class="nx">collection</span><span class="p">.</span><span class="nf">createIndex</span><span class="p">({</span> <span class="na">fieldName</span><span class="p">:</span> <span class="mi">1</span> <span class="p">})</span>   <span class="c1">// ascending</span>
<span class="nx">db</span><span class="p">.</span><span class="nx">collection</span><span class="p">.</span><span class="nf">createIndex</span><span class="p">({</span> <span class="na">fieldName</span><span class="p">:</span> <span class="o">-</span><span class="mi">1</span> <span class="p">})</span>  <span class="c1">// descending</span>
</code></pre></div></div> <p>Without an index, every query does a <strong>COLLSCAN</strong>, reading every document. $O(n)$. With an index, it’s a B+ tree lookup: $O(\log n)$.</p> <p align="center"> <img src="/assets/2026-03-07-17-00-39.png" width="850"/> </p> <h3 id="index-types">Index Types</h3> <table> <thead> <tr> <th>Type</th> <th>Example</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td>Single field</td> <td><code class="language-plaintext highlighter-rouge">{ age: 1 }</code></td> <td>Index on one field</td> </tr> <tr> <td>Compound</td> <td><code class="language-plaintext highlighter-rouge">{ status: 1, createdAt: -1 }</code></td> <td>Multi-field; order matters</td> </tr> <tr> <td>Unique</td> <td><code class="language-plaintext highlighter-rouge">{ email: 1 }, { unique: true }</code></td> <td>Enforces uniqueness</td> </tr> <tr> <td>Text</td> <td><code class="language-plaintext highlighter-rouge">{ description: "text" }</code></td> <td>Full-text search</td> </tr> <tr> <td>TTL</td> <td><code class="language-plaintext highlighter-rouge">{ createdAt: 1 }, { expireAfterSeconds: 3600 }</code></td> <td>Auto-deletes documents after a time period</td> </tr> </tbody> </table> <p><strong>Trade-off</strong>: Indexes speed up reads but slow down writes (every insert/update/delete must also update the index). They also consume memory. Index based on your actual query patterns.</p> <h3 id="the-leftmost-prefix-rule">The Leftmost Prefix Rule</h3> <p>For a compound index <code class="language-plaintext highlighter-rouge">{ status: 1, customerId: 1, createdAt: -1 }</code>, MongoDB can only use it if the query includes fields <strong>starting from the left</strong>.</p> <p align="center"> <img src="/../assets/2026-03-07-17-02-24.png" width="850"/> </p> <p>It’s like looking up a first name in a phone book sorted by last name first: you have to start from the left.</p> <h3 id="the-esr-rule">The ESR Rule</h3> <p>When designing compound indexes, order fields by:</p> <pre><code class="language-mermaid">graph LR
    E["&lt;b&gt;E&lt;/b&gt;quality&lt;br/&gt;Exact match fields"] --&gt; S["&lt;b&gt;S&lt;/b&gt;ort&lt;br/&gt;Sort fields"]
    S --&gt; R["&lt;b&gt;R&lt;/b&gt;ange&lt;br/&gt;Range/inequality fields"]

    style E fill:#4ecdc4,stroke:#333,color:#000
    style S fill:#ffe66d,stroke:#333,color:#000
    style R fill:#ff6b6b,stroke:#333,color:#000
</code></pre> <p>This gives the query planner the best chance of efficient index usage.</p> <h3 id="selectivity-and-the-query-planner">Selectivity and the Query Planner</h3> <p><strong>Selectivity</strong> = matching documents / total documents. High selectivity (few matches) makes indexes valuable. Low selectivity (e.g., 80% of docs match) may cause the planner to prefer a COLLSCAN.</p> <p>The query planner <strong>races candidate plans</strong> on first execution, caches the winner by query shape. The cache invalidates after significant writes, index changes, or server restarts.</p> <p><strong>Known gotcha</strong>: plans are cached by query <em>shape</em>, not values. A COLLSCAN cached for a low-selectivity value (<code class="language-plaintext highlighter-rouge">status: "active"</code> matching 80% of docs) will also be used for a high-selectivity value (<code class="language-plaintext highlighter-rouge">status: "suspended"</code> matching 0.1%). Workarounds include <code class="language-plaintext highlighter-rouge">.hint()</code> and compound indexes.</p> <p>Compound indexes improve selectivity: even if no single field is selective, the combination can be. <code class="language-plaintext highlighter-rouge">{ status: "active", region: "Mumbai" }</code> might narrow 500M docs to 25M (5%) vs 400M (80%) for status alone.</p> <hr/> <h2 id="q6---explain-embedding-vs-referencing-what-factors-influence-the-choice-medium">Q6 - Explain embedding vs. referencing. What factors influence the choice? <span class="badge-difficulty badge-medium">medium</span></h2> <p><strong>Embedding</strong>: nest related data directly inside the parent document. Larger documents, but a single I/O call to get everything.</p> <p><strong>Referencing</strong>: store a reference (like a foreign key) and do a separate lookup. Smaller documents, but an additional round trip.</p> <pre><code class="language-mermaid">graph TB
    subgraph "Embedding"
        direction TB
        U1["User Document"]
        U1 --- UA["{ name: 'Vishal',&lt;br/&gt;address: {&lt;br/&gt;&amp;nbsp;&amp;nbsp;street: '123 Main',&lt;br/&gt;&amp;nbsp;&amp;nbsp;city: 'Mumbai'&lt;br/&gt;},&lt;br/&gt;orders: [&lt;br/&gt;&amp;nbsp;&amp;nbsp;{ item: 'Book', amount: 25 },&lt;br/&gt;&amp;nbsp;&amp;nbsp;{ item: 'Pen', amount: 5 }&lt;br/&gt;] }"]
    end

    subgraph "Referencing"
        direction TB
        U2["User Document"] --- UD2["{ name: 'Vishal',&lt;br/&gt;address_id: ObjectId(...) }"]
        A2["Address Document"] --- AD2["{ _id: ObjectId(...),&lt;br/&gt;street: '123 Main',&lt;br/&gt;city: 'Mumbai' }"]
        U2 -. "$lookup" .-&gt; A2
    end
</code></pre> <h3 id="when-to-embed">When to Embed</h3> <ul> <li><strong>One-to-one</strong> or <strong>one-to-few</strong> relationships (user with address, order with line items)</li> <li>Data that’s always accessed together</li> <li>The child data doesn’t make sense on its own</li> </ul> <h3 id="when-to-reference">When to Reference</h3> <ul> <li><strong>One-to-many</strong> or <strong>many-to-many</strong> with large cardinalities (a user with 10,000 comments would bloat toward the 16 MB limit)</li> <li>The child entity is accessed independently</li> <li>The referenced data changes frequently. With embedding, you’d update it everywhere; with referencing, update in one place</li> </ul> <blockquote> <p>If you find yourself doing <code class="language-plaintext highlighter-rouge">$lookup</code> on every query across five collections, you’ve probably picked the wrong database or the wrong schema design. It’s a spectrum, not a binary choice.</p> </blockquote> <hr/> <h2 id="q7---what-is-an-aggregation-pipeline-walk-through-an-example-medium">Q7 - What is an aggregation pipeline? Walk through an example. <span class="badge-difficulty badge-medium">medium</span></h2> <p>An aggregation pipeline is an array of <strong>stages</strong>, where each stage transforms data and passes its output to the next, like Unix pipes (<code class="language-plaintext highlighter-rouge">cat | grep | sort | uniq</code>).</p> <h3 id="example-total-revenue-per-customer-from-an-orders-collection">Example: Total revenue per customer from an orders collection</h3> <pre><code class="language-mermaid">graph LR
    A["orders&lt;br/&gt;collection"] --&gt; B["$match&lt;br/&gt;status = completed"]
    B --&gt; C["$group&lt;br/&gt;by customerId&lt;br/&gt;sum(amount)"]
    C --&gt; D["$sort&lt;br/&gt;totalRevenue DESC"]
    D --&gt; E["$limit&lt;br/&gt;top 10"]

    style A fill:#e8e8e8,stroke:#333
    style B fill:#4ecdc4,stroke:#333,color:#000
    style C fill:#ffe66d,stroke:#333,color:#000
    style D fill:#ff6b6b,stroke:#333,color:#000
    style E fill:#c9b1ff,stroke:#333,color:#000
</code></pre> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">db</span><span class="p">.</span><span class="nx">orders</span><span class="p">.</span><span class="nf">aggregate</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">$match</span><span class="p">:</span>  <span class="p">{</span> <span class="na">status</span><span class="p">:</span> <span class="dl">"</span><span class="s2">completed</span><span class="dl">"</span> <span class="p">}</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">$group</span><span class="p">:</span>  <span class="p">{</span> <span class="na">_id</span><span class="p">:</span> <span class="dl">"</span><span class="s2">$customerId</span><span class="dl">"</span><span class="p">,</span> <span class="na">totalRevenue</span><span class="p">:</span> <span class="p">{</span> <span class="na">$sum</span><span class="p">:</span> <span class="dl">"</span><span class="s2">$amount</span><span class="dl">"</span> <span class="p">}</span> <span class="p">}</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">$sort</span><span class="p">:</span>   <span class="p">{</span> <span class="na">totalRevenue</span><span class="p">:</span> <span class="o">-</span><span class="mi">1</span> <span class="p">}</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">$limit</span><span class="p">:</span>  <span class="mi">10</span> <span class="p">}</span>
<span class="p">])</span>
</code></pre></div></div> <h3 id="common-stages">Common Stages</h3> <table> <thead> <tr> <th>Stage</th> <th>SQL Equivalent</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">$match</code></td> <td><code class="language-plaintext highlighter-rouge">WHERE</code></td> <td>Filter documents</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">$group</code></td> <td><code class="language-plaintext highlighter-rouge">GROUP BY</code></td> <td>Aggregate values</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">$sort</code></td> <td><code class="language-plaintext highlighter-rouge">ORDER BY</code></td> <td>Order results</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">$project</code></td> <td><code class="language-plaintext highlighter-rouge">SELECT</code></td> <td>Reshape / select fields</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">$unwind</code></td> <td><code class="language-plaintext highlighter-rouge">LATERAL FLATTEN</code></td> <td>Flatten arrays into one doc per element</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">$lookup</code></td> <td><code class="language-plaintext highlighter-rouge">JOIN</code></td> <td>Join with another collection</td> </tr> </tbody> </table> <p><strong>Performance tip</strong>: put <code class="language-plaintext highlighter-rouge">$match</code> stages as early as possible to reduce the volume of documents flowing through later stages. MongoDB can use indexes on early <code class="language-plaintext highlighter-rouge">$match</code> stages and may merge/reorder stages internally.</p> <hr/> <h2 id="q8---what-are-read-concerns-and-write-concerns-how-do-they-affect-consistency-and-durability-medium">Q8 - What are read concerns and write concerns? How do they affect consistency and durability? <span class="badge-difficulty badge-medium">medium</span></h2> <p>Read and write concerns let you <strong>tune the trade-off</strong> between consistency/durability and latency/availability.</p> <h3 id="write-concern">Write Concern</h3> <pre><code class="language-mermaid">sequenceDiagram
    participant App
    participant Primary
    participant Secondary1
    participant Secondary2

    Note over App,Secondary2: w: 1 (default)
    App-&gt;&gt;Primary: write
    Primary--&gt;&gt;App: ack ✅
    Primary--)Secondary1: replicate (async)
    Primary--)Secondary2: replicate (async)

    Note over App,Secondary2: w: "majority"
    App-&gt;&gt;Primary: write
    Primary--)Secondary1: replicate
    Secondary1--&gt;&gt;Primary: ack
    Primary--&gt;&gt;App: ack ✅ (2/3 confirmed)
    Primary--)Secondary2: replicate (async)
</code></pre> <table> <thead> <tr> <th>Write Concern</th> <th>Behavior</th> <th>Trade-off</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">w: 0</code></td> <td>Fire and forget</td> <td>Max throughput, no safety</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">w: 1</code> (default)</td> <td>Primary acknowledges</td> <td>Fast, but data lost if primary dies before replication</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">w: "majority"</code></td> <td>Majority of nodes confirm</td> <td>Slower, but durable</td> </tr> </tbody> </table> <h3 id="read-concern">Read Concern</h3> <table> <thead> <tr> <th>Read Concern</th> <th>Behavior</th> <th>Risk</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">"local"</code> (default)</td> <td>Read whatever the node has</td> <td>May read data that gets rolled back</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">"majority"</code></td> <td>Only read majority-committed data</td> <td>No phantom reads</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">"linearizable"</code></td> <td>Most recent majority-committed write</td> <td>Slowest; strongest guarantee</td> </tr> </tbody> </table> <h3 id="relationship-to-cap">Relationship to CAP</h3> <p>MongoDB is <strong>CP-leaning</strong>: the primary rejects writes if it can’t reach a majority, favoring <strong>consistency</strong> over availability. Read/write concerns let you slide along the consistency-latency spectrum within that framework.</p> <hr/> <h2 id="q9---500m-documents-queries-are-slow-explain-shows-collscan-indexes-exist-but-arent-used-diagnose-and-fix-hard">Q9 - 500M documents, queries are slow, <code class="language-plaintext highlighter-rouge">explain()</code> shows COLLSCAN, indexes exist but aren’t used. Diagnose and fix. <span class="badge-difficulty badge-hard">hard</span></h2> <pre><code class="language-mermaid">flowchart TD
    A[Slow Query with COLLSCAN] --&gt; B{Run explain&lt;br/&gt;executionStats}

    B --&gt; C{Index exists&lt;br/&gt;for query fields?}
    C -- No --&gt; C1[Create index matching&lt;br/&gt;query pattern]

    C -- Yes --&gt; D{Check field order&lt;br/&gt;in compound index}
    D -- "Wrong order&lt;br/&gt;(leftmost prefix)" --&gt; D1[Reorder compound&lt;br/&gt;index fields]

    D -- Correct --&gt; E{Check for&lt;br/&gt;index-defeating operators}
    E -- "$ne, $not,&lt;br/&gt;$regex ^.*" --&gt; E1[Rewrite query to&lt;br/&gt;avoid these operators]

    E -- None --&gt; F{Check type&lt;br/&gt;mismatches}
    F -- "String vs Number" --&gt; F1[Fix data types&lt;br/&gt;in query or documents]

    F -- Types OK --&gt; G{Check selectivity}
    G -- "Low selectivity&lt;br/&gt;(80%+ match)" --&gt; G1["Add more fields to&lt;br/&gt;compound index, or&lt;br/&gt;use .hint()"]
    G -- High selectivity --&gt; H{Index in RAM?}
    H -- No --&gt; H1[Increase available RAM&lt;br/&gt;or reduce index size]
    H -- Yes --&gt; I[Check for index&lt;br/&gt;corruption: reIndex]
</code></pre> <h3 id="query-side-problems">Query-Side Problems</h3> <ul> <li><strong>Unindexed fields</strong>: the query filters on fields the index doesn’t cover</li> <li><strong>Wrong field order</strong> in compound indexes. If the index is <code class="language-plaintext highlighter-rouge">{ status: 1, age: 1 }</code> but the query only filters on <code class="language-plaintext highlighter-rouge">age</code>, the leftmost prefix rule prevents usage</li> <li><strong>Index-defeating operators</strong>: <code class="language-plaintext highlighter-rouge">$ne</code>, <code class="language-plaintext highlighter-rouge">$not</code>, <code class="language-plaintext highlighter-rouge">$regex</code> with leading wildcard. These force a COLLSCAN</li> <li><strong>Type mismatches</strong>: field stored as string but queried as number</li> </ul> <h3 id="index-side-problems">Index-Side Problems</h3> <ul> <li><strong>Index corruption</strong>: rare, but fixable with <code class="language-plaintext highlighter-rouge">reIndex()</code></li> <li><strong>Index not in memory</strong>: with 500M documents, the index may be too large for RAM; the planner may decide COLLSCAN is cheaper</li> </ul> <h3 id="data-side-problems">Data-Side Problems</h3> <ul> <li><strong>Low selectivity</strong>: if an index on <code class="language-plaintext highlighter-rouge">status</code> has only two values and 80% are <code class="language-plaintext highlighter-rouge">"active"</code>, the planner decides scanning is cheaper</li> </ul> <h3 id="diagnostic-tools">Diagnostic Tools</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">explain("executionStats")</code></strong>: which plan was chosen, indexes considered and rejected, and why</li> <li><strong><code class="language-plaintext highlighter-rouge">$indexStats</code></strong>: usage frequency per index</li> <li><strong><code class="language-plaintext highlighter-rouge">mongotop</code> / <code class="language-plaintext highlighter-rouge">mongostat</code></strong>: server-level performance</li> <li><strong>Rejected plans</strong> in explain output: alternatives the planner considered</li> </ul> <hr/> <h2 id="q10---how-does-mongodb-handle-transactions-across-multiple-shards-what-are-the-limitations-hard">Q10 - How does MongoDB handle transactions across multiple shards? What are the limitations? <span class="badge-difficulty badge-hard">hard</span></h2> <h3 id="single-shard-the-easy-case">Single-Shard: The Easy Case</h3> <p>If all documents in a transaction live on the same shard, it behaves like a single-replica-set transaction. One node coordinates, no cross-network overhead. This is the ideal case.</p> <h3 id="cross-shard-two-phase-commit">Cross-Shard: Two-Phase Commit</h3> <p>When a transaction spans multiple shards, MongoDB uses a <strong>two-phase commit</strong> protocol:</p> <pre><code class="language-mermaid">sequenceDiagram
    participant App
    participant mongos as mongos (Router)
    participant Shard_A as Shard A
    participant Shard_B as Shard B

    App-&gt;&gt;mongos: startTransaction()
    App-&gt;&gt;mongos: update doc on Shard A
    mongos-&gt;&gt;Shard_A: tentative write (held in memory)
    App-&gt;&gt;mongos: update doc on Shard B
    mongos-&gt;&gt;Shard_B: tentative write (held in memory)

    App-&gt;&gt;mongos: commitTransaction()

    rect rgb(173, 216, 230)
        Note over mongos,Shard_B: Phase 1 — Prepare
        mongos-&gt;&gt;Shard_A: prepare
        Shard_A-&gt;&gt;Shard_A: validate + write prepare&lt;br/&gt;record to oplog
        Shard_A--&gt;&gt;mongos: prepared ✅
        mongos-&gt;&gt;Shard_B: prepare
        Shard_B-&gt;&gt;Shard_B: validate + write prepare&lt;br/&gt;record to oplog
        Shard_B--&gt;&gt;mongos: prepared ✅
    end

    rect rgb(144, 238, 144)
        Note over mongos,Shard_B: Phase 2 — Commit
        mongos-&gt;&gt;mongos: write commit decision&lt;br/&gt;to oplog (point of no return)
        mongos-&gt;&gt;Shard_A: commit
        mongos-&gt;&gt;Shard_B: commit
        Shard_A--&gt;&gt;mongos: committed ✅
        Shard_B--&gt;&gt;mongos: committed ✅
    end

    mongos--&gt;&gt;App: transaction committed ✅
</code></pre> <p><strong>Phase 1 (Prepare)</strong>: Each shard validates its operations, writes a <strong>durable prepare record</strong> to its oplog, holds write locks on affected documents, and responds “prepared” or “abort.” Prepare records are crash-safe. If a shard restarts, it reads the oplog and asks the coordinator whether to commit or abort.</p> <p><strong>Phase 2 (Commit)</strong>: Once all shards confirm prepared, the coordinator writes a <strong>commit decision</strong> to its oplog (point of no return) and tells all shards to finalize. Other readers see pre-transaction state until commit via snapshot isolation.</p> <h3 id="what-can-go-wrong">What Can Go Wrong</h3> <ul> <li><strong>Write conflicts</strong>: another operation touched the same document</li> <li><strong>Lock timeouts</strong>: locks held too long waiting for prepare</li> <li><strong>Node failures</strong>: triggers abort and cleanup on recovery</li> <li><strong>Snapshot too old</strong>: transaction took too long, snapshot expired</li> <li><strong>mongos crash before prepare</strong>: nothing durable was written, tentative changes cleaned up on session timeout</li> </ul> <h3 id="performance-cost-vs-single-shard">Performance Cost vs. Single-Shard</h3> <pre><code class="language-mermaid">graph LR
    subgraph "Single-Shard Transaction"
        A1[1 network hop] --&gt; B1[Short lock duration] --&gt; C1[Low abort risk]
    end
    subgraph "Cross-Shard Transaction"
        A2[Multiple network&lt;br/&gt;round trips] --&gt; B2[Longer lock holds&lt;br/&gt;across 2PC] --&gt; C2[Higher abort risk&lt;br/&gt;more failure points]
    end

    style A1 fill:#d4edda,stroke:#333
    style B1 fill:#d4edda,stroke:#333
    style C1 fill:#d4edda,stroke:#333
    style A2 fill:#f8d7da,stroke:#333
    style B2 fill:#f8d7da,stroke:#333
    style C2 fill:#f8d7da,stroke:#333
</code></pre> <p>The best way to handle cross-shard transactions is to <strong>design your schema so you rarely need them</strong>. Choose the right shard key by sharding on the entity that ties your most common transactions together. Sharding both <code class="language-plaintext highlighter-rouge">orders</code> and <code class="language-plaintext highlighter-rouge">customers</code> by <code class="language-plaintext highlighter-rouge">customerId</code> keeps each customer’s data colocated. Embed related data instead of referencing when it’s always accessed together, since a single atomic document update avoids transactions entirely. Denormalize by copying needed fields from one entity into another, trading storage/update complexity for avoiding cross-collection lookups.</p> <blockquote> <p>The shard key is the most important decision in a sharded MongoDB deployment. The two-phase commit exists as a safety net, not a primary access pattern.</p> </blockquote>]]></content><author><name></name></author><category term="database-internals"/><category term="database-internals"/><category term="mongodb"/><category term="nosql"/><summary type="html"><![CDATA[10 questions that walk through MongoDB internals: documents, indexing, schema design, aggregation pipelines, consistency tuning, and cross-shard transactions.]]></summary></entry><entry><title type="html">Skip Lists</title><link href="https://vgraman0.github.io/blog/2026/skip-lists/" rel="alternate" type="text/html" title="Skip Lists"/><published>2026-02-28T00:00:00+00:00</published><updated>2026-02-28T00:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/skip-lists</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/skip-lists/"><![CDATA[<p>In order to build the MemTable for my <a href="https://github.com/vgraman0/RustyLSM-Storage-Engine">Rusty LSM-Tree</a>, I used skip lists.</p> <p>Skip lists are typically preferred over balanced binary search trees since storage engines are highly concurrent.</p> <ul> <li>Rebalancing a binary tree would require a global lock that would add a lot of overhead.</li> <li>Insertion in a skip list can be done using lock-free or wait-free algos (using compare-and-swap).</li> </ul> <h2 id="skip-lists">Skip Lists</h2> <p>A skip list is a probabilistic data structure that provides $O(\log n)$ average-case search, insertion, and deletion within a sorted list of elements.</p> <p align="center"> <img src="/assets/img/skip-lists/image-21.png" width="650"/> </p> <h3 id="search">Search</h3> <p>We always start at the top-most layer at the sentinel node.</p> <ol> <li>If the next node’s key is less than of equal to the key we are looking for,we hop to that node.</li> <li>If the next node’s key is greater than our target (or if there is no next node), we drop down one level to the layer below and try moving forward again.</li> <li>Repeat this until we reach the bottom layer. If the key is not found on layer 0, then it does not exist in the list.</li> </ol> <h3 id="insertion">Insertion</h3> <ol> <li>Search for the key. As we move right and drop down, we keep track of the last node we visited at every level.</li> <li>Pick a random height $H$ for the new node.</li> <li>For every level from $0$ to $H-1$, we point the new node’s <code class="language-plaintext highlighter-rouge">next</code> to the node that the update neighbor was previously pointing to. Then, we point the update neighbor to our new node.</li> </ol> <h3 id="deletion">Deletion</h3>]]></content><author><name></name></author><category term="system-design"/><category term="database-internals"/><category term="rust"/><summary type="html"><![CDATA[Skip Lists Memtables for LSM Trees]]></summary></entry><entry><title type="html">HTTP</title><link href="https://vgraman0.github.io/blog/2026/http/" rel="alternate" type="text/html" title="HTTP"/><published>2026-02-21T00:00:00+00:00</published><updated>2026-02-21T00:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/http</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/http/"><![CDATA[<p>The <strong>HyperText Transfer Protocol</strong> (HTTP) is how browsers communicate to web servers.</p> <ul> <li>HTTP is an <strong>application-layer</strong> protocol (Layer 7) that runs on top of a transport protocol (<strong>TCP</strong>, or <strong>QUIC</strong> in HTTP/3)</li> <li>It follows a <strong>request-response</strong> model: A client sends a request, the server sends back a response.</li> </ul> <p>HTTP has evolved significantly since inception. The goal of this blog post is to give a history of the evolution and describe the features and limitations of each version.</p> <p align="center"> <img src="/assets/img/http/image-17.png" width="650"/> </p> <h2 id="early-http-versions">Early HTTP Versions</h2> <p>HTTP/0.9 was introduced by <strong>Tim Berners-Lee</strong> as part of the <strong>World Wide Web</strong>. It only supported <code class="language-plaintext highlighter-rouge">GET</code> requests and did not have headers or status codes (<code class="language-plaintext highlighter-rouge">200</code>, <code class="language-plaintext highlighter-rouge">404</code>, <code class="language-plaintext highlighter-rouge">500</code>, etc.). These were introduced in HTTP/1.0 along with <code class="language-plaintext highlighter-rouge">POST</code> and <code class="language-plaintext highlighter-rouge">HEAD</code>.</p> <p align="center"> <img src="/assets/img/http/image-4.png" width="650"/> </p> <p>The original versions of HTTP did not support <strong>persistent connections</strong>. This meant that every request would require a new connection, which comes with additional overhead.</p> <p align="center"> <img src="/assets/img/http/image-18.png" width="650"/> </p> <p>In HTTP/1.0, a connection was required for every resource. This means every image, CSS file, or JavaScript file required a new connection (and corresponding handshakes).</p> <h2 id="http11-1997">HTTP/1.1 (1997)</h2> <p>HTTP/1.1 addressed many of the initial problems with HTTP/1.0, and is still widely used for simple websites.</p> <h3 id="improvements">Improvements</h3> <ul> <li><strong>Persistent Connections</strong>: Instead of closing the connection after a single response, the server keeps the TCP socket open. This allows the client to send multiple requests through the same pipe without the overhead of repeated handshakes.</li> </ul> <p align="center"> <img src="/assets/img/http/image-11.png" width="650"/> </p> <ul> <li><strong>Chunked transfer encoding</strong>: Servers could send results in smaller chunks. This made page rendering faster, since the server didn’t have to wait for the whole response to be ready before sending chunks.</li> </ul> <p align="center"> <img src="/assets/img/http/image-14.png" width="650"/> </p> <ul> <li><strong>Pipelining</strong>: Instead of waiting for a response before sending the next request, the client could send multiple requests at once.</li> </ul> <p align="center"> <img src="/assets/img/http/image-12.png" width="650"/> </p> <h3 id="head-of-line-blocking">Head-of-Line Blocking</h3> <p><strong>Pipelining</strong> failed in practice due to <strong>head-of-line blocking</strong>.</p> <p align="center"> <img src="/assets/img/http/image-16.png" width="650"/> </p> <ul> <li>The server must send responses in the same order that they were received.</li> <li>If the first result gets stuck, all subsequent requests are also blocked, even if they are ready to be sent.</li> </ul> <p>Developers got around this using a few methods:</p> <ul> <li><strong>Domain sharding</strong>: Serve static assets from subdomains (with separate connections).</li> <li>Make fewer requests by <strong>bundling assets</strong> together.</li> </ul> <h2 id="http2-2015">HTTP/2 (2015)</h2> <p>The major goal of HTTP/2 was to reduce latency and address head-of-line blocking at the application layer.</p> <h3 id="improvements-1">Improvements</h3> <p><strong>Binary Framing</strong>: Instead of plain text messages, HTTP/2 splits messages into binary frames.</p> <ul> <li><code class="language-plaintext highlighter-rouge">HEADER</code> frames carry the metadata for the request/reponse. <ul> <li>These are encoded using HPACK: Headers are indexed in a dictionary. Instead of sending a repeated header, the browser can send the index and the server will look up the corresponding header.</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">DATA</code> frames contain the actual message.</li> </ul> <p><strong>Multiplexing</strong>: Multiple streams (request/response pairs) are interleaved over a single TCP connection.</p> <p align="center"> <img src="/assets/img/http/image-19.png" width="650"/> </p> <p>This avoids the application level head-of-line blocking problem. We can use a single connection with frames from separate streams.</p> <p>However, we still have TCP-level head-of-line blocking. If a frame is dropped on the response, then all the frames behind it are also not received.</p> <h2 id="sources">Sources</h2> <p>Original HTTP Memos:</p> <ul> <li><a href="https://datatracker.ietf.org/doc/html/rfc1945">RFC 1945 — HTTP/1.0</a></li> <li><a href="https://datatracker.ietf.org/doc/html/rfc2616">RFC 2616 — HTTP/1.1</a></li> <li><a href="https://datatracker.ietf.org/doc/html/rfc7540">RFC 7540 — HTTP/2</a></li> <li><a href="https://datatracker.ietf.org/doc/html/rfc9114">RFC 9114 — HTTP/3</a></li> </ul> <p>Additional Resources:</p> <ul> <li><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Evolution_of_HTTP">MDN — Evolution of HTTP</a></li> <li><a href="https://www.youtube.com/watch?v=UMwQjFzTQXw">HTTP/1 to HTTP/2 to HTTP/3 (YouTube)</a></li> </ul>]]></content><author><name></name></author><category term="system-design"/><category term="networking"/><category term="http"/><category term="networking"/><summary type="html"><![CDATA[HTTP/1.0 through HTTP/3 — what changed and why]]></summary></entry><entry><title type="html">5. Replication</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch5/" rel="alternate" type="text/html" title="5. Replication"/><published>2026-02-13T00:00:00+00:00</published><updated>2026-02-13T00:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch5</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch5/"><![CDATA[<p>Replication means keeping a copy of the same data on multiple machines that are connected via a network. <img src="/assets/img/ddia/ch5-image-2.png" alt="alt text"/> It comes with a number of benefits:</p> <ol> <li>Availability: Replication allows a system to continue function even if parts of it fail. When one machine fails, another can take over.</li> <li>Read throughput: For workloads that are “read-heavy”, we can scale the number of machines that serve queries (horizontal scaling), allowing us to distribute the read requests across those machines.</li> <li>Reducing latency: We can place copies of the data geographically closer to the users, reducing the time they spend waiting for network packets to travel to their location.</li> </ol> <h2 id="single-leader-partitioning">Single-Leader Partitioning</h2> <p>We start with several nodes that each store a copy of the database (replicas). These are split up into two categories:</p> <ul> <li><strong>Leader (Master/Primary)</strong>: The exclusive entry point for <strong>writes</strong>. When a client wants to change data, the request must go to the leader, which writes the change to its local storage first.</li> <li><strong>Followers (Read Replicas/Slaves)</strong>: Read-only nodes from the client’s perspective. They stay up to date by receiving a replication log from the leader. They apply every write in the same order that the leader processed them.</li> </ul> <p><img src="/assets/img/ddia/ch5-image-1.png" alt="alt text"/></p> <p>An important question to answer when designing the system is whether the replication stream should be synchronous and asynchronous.</p> <ul> <li><strong>Synchronous Replication</strong>: The leader waits for the follower to confirm that it has received and written the data to its local storage before reporting success to the client. <ul> <li><strong>Pros</strong>: The follower is guaranteed to have an up-to-date copy of the data that is <strong>consistent</strong> with the leader. If the leader fails, you can be certain that the data exists on the follower.</li> <li><strong>Cons</strong>: If the synchronous follower becomes unreachable (due to a crash or network fault), the leader cannot process any more writes. It <strong>must block</strong> until the replica is available again.</li> <li>In practice, people follow a <strong>semi-synchronous</strong> setup, where only one follower is synchronous while others remain asynchronous.</li> </ul> </li> <li><strong>Asynchronous Replication</strong>: The leader sends the data change message to its followers but notifies the client of success immediately without waiting for a response. <ul> <li><strong>Pros</strong>: High availability: The leader can continue processing writes even if all followers fall behind or the network is interrupted.</li> <li><strong>Cons</strong>: Low durability: If the leader fails and cannot be recovered, any writes that were not yet replicated are lost forever.</li> </ul> </li> </ul> <h3 id="setting-up-new-followers">Setting up new followers</h3> <p>Copying files from one node to another isn’t enough since writes are constantly happening. The actual process looks like this:</p> <ol> <li>Take a consistent snapshot of the leader’s database without locking the system.</li> <li>Move the snapshot to the new follower node.</li> <li>The new follower connects to the leader and requests all the data changes since the snapshot was taken. This requires the snapshot to be tied to a specific position in the replication log.</li> <li>Once the follower processes the backlog of changes, it has caught up and can continue receiving the live stream of data changes.</li> </ol> <h3 id="node-outages">Node outages</h3> <p>If a follower goes down, it is relatively easy to fix.</p> <ul> <li>Followers keep a local log of all the changes it has received. So it knows the last transaction it sucessfully processed before the fault.</li> <li>When it restarts, it connects to the leader, requests the missing data since that point, and catches up.</li> </ul> <p>If a leader fails, it is much more complex because the system must choose a new leader and redirect all traffic.</p> <ul> <li>Detection: Nodes ping each other; if the leader doesn’t respond for a period (often a 30-second timeout), it is assumed to be dead.</li> <li>A new leader is chosen, typically the replica with the most up-to-date data to minimize loss.</li> <li>Clients are updated to send writes to the new leader.</li> <li>If the old leader comes back, it is forced to be a follower.</li> </ul> <p>This process is dangerous for a few reasons:</p> <ul> <li>In asynchronous setups, the new leader might not have received all the writes before the old leader died. The common solution is to discard those unreplicated writes.</li> <li>Split brain: Two nodes might both believe they are the leader. If both accept writes, data will be corrupted or lost.</li> <li>A timeout that’s too short can cause unnecessary failovers during temporary load spikes.</li> </ul> <h3 id="replication-logs">Replication Logs</h3> <p>There are several ways to format the replication log that the leader sends to its followers.</p> <p><strong>Statement-based Replication</strong>: The leader logs every write request (<code class="language-plaintext highlighter-rouge">INSERT</code>, <code class="language-plaintext highlighter-rouge">UPDATE</code>, <code class="language-plaintext highlighter-rouge">DELETE</code>) and sends them to the followers to execute.</p> <ul> <li>Pros: Very compact.</li> <li>Cons: <ul> <li>Breaks with nondeterminism. Functions like <code class="language-plaintext highlighter-rouge">NOW()</code> or <code class="language-plaintext highlighter-rouge">RAND()</code> will generate different values on each replica.</li> <li>Autoincrementing columns (<code class="language-plaintext highlighter-rouge">UPDATE ... WHERE &lt;some condition&gt;</code>) or statements with side effects (triggers, stored procedures) must be executed in the exact same order on every replica. This is difficult to guarantee with concurrent transactions.</li> </ul> </li> </ul> <p>Examples: MySQL (versions &lt; 5.1).</p> <p><strong>Write-Ahead Log (WAL) Shipping</strong>: Every database uses a log for its storage engine (B-Trees or LSM-Trees). The leader sends this exact low-level log to its followers.</p> <ul> <li>Pros: Very efficient as it already uses the same log for crash recovery.</li> <li>Cons: The log is tightly coupled to the storage engine. If the database version changes its disk format, the leader and follower cannot run different versions of the software. <ul> <li>It requires downtime for upgrades because you cannot perform rolling upgrades.</li> </ul> </li> </ul> <p>Examples: PostgreSQL, Oracle</p> <p><strong>Logical (row-based) Log Replication</strong>: It uses a sequence of records describing writes at the granularity of a row.</p> <ul> <li>For an inserted row: The log contains all new column values.</li> <li>For a deleted row: The log contains the primary key or enough info to identify the row.</li> <li>For an updated row: The log contains the identifier and the new values of the changed columns.</li> </ul> <p>Benefits:</p> <ul> <li>Because it is “logical”, it will stay backward compatible. This makes it possible to do rolling upgrades.</li> <li>It is easier for external applications to parse.</li> </ul> <p>Examples: MySQL</p> <p><strong>Trigger-based Replication</strong>: Useful when you need custom logic (like replicating a subset of data or between different types of DBs).</p> <ul> <li>Application-defined code automatically executes during a write transaction to log the change into a separate table. An external process can read this and replicate.</li> <li>This has significantly higher overhead and is more prone to bugs.</li> </ul> <p>Examples: Databus for Oracle, Bucardo for Postgres</p> <h3 id="dealing-with-replication-lag">Dealing with Replication Lag</h3> <p>When you scale reads using asynchronous replication, you gain availability but lose strong consistency (in favor of eventual consistency).</p> <p>While the replication lag is usually less than a second, it can spike to minutes under heavy load or network issues. This can lead to anomalies that require special handling.</p> <h4 id="read-after-write-consistency">Read-After-Write Consistency</h4> <p>Many applications allow users to submit data and then view it immediately. If the write goes to the leader but the subsequent read goes to a stale follower, the user will think the data is lost.</p> <p>The guarantee that users can immediately see updates they submitted after reloading is called <strong>read-after-write consistency</strong>.</p> <p>Some potential implementations:</p> <ul> <li><strong>Leader-Routing</strong>: Always read a user’s own profile from the leader, but read other users’ profiles from a follower.</li> <li><strong>Time-based Buffering</strong>: For one minute after the last update, make all reads from the leader.</li> <li><strong>Timestamp Tracking</strong>: The client remembers the timestamp of its last write, and the system ensures that it only reads from a replica that is updated at least until that timestamp.</li> </ul> <h4 id="monotonic-reads">Monotonic Reads</h4> <p>An anomaly where a user sees things “moving backward in time”, because their requests are routed to different followers (with didfferent amounts of lag).</p> <p><strong>Sticky Routing</strong>: Ensure each user always reads from the same replica (e.g. hasing the user ID).</p> <h4 id="consistent-prefix-reads">Consistent Prefix Reads</h4> <p>This guarantee says that if a sequence of writes happens in a certain order, then anyone reading those writes will see them in the same order.</p> <p>This is a particular problem in sharded databases, since different partitions operate independently.</p> <p><strong>Causal Partitioning</strong>: Ensure that all writes that are causally related are written to the same partition.</p> <h2 id="multi-leader-partitioning">Multi-Leader Partitioning</h2>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><summary type="html"><![CDATA[Replication means keeping a copy of the same data on multiple machines that are connected via a network. It comes with a number of benefits: Availability: Replication allows a system to continue function even if parts of it fail. When one machine fails, another can take over. Read throughput: For workloads that are “read-heavy”, we can scale the number of machines that serve queries (horizontal scaling), allowing us to distribute the read requests across those machines. Reducing latency: We can place copies of the data geographically closer to the users, reducing the time they spend waiting for network packets to travel to their location.]]></summary></entry><entry><title type="html">4. Encoding and Evolution</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch4/" rel="alternate" type="text/html" title="4. Encoding and Evolution"/><published>2026-02-04T00:00:00+00:00</published><updated>2026-02-04T00:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch4</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch4/"><![CDATA[<p>One of the core requirements for data systems is evolvability. Features are added over time, requirements are better understood, or business needs change.</p> <p><strong>Data models and schema flexibility</strong>: Changes to the application can require a change to the underlying data.</p> <ul> <li>Relational databases follow a “one schema rule”. Although the schema can be changed, only one schema is enforced at a given time.</li> <li>Schema-on-read databases do not enforce a fixed schema. This allows the database to contain a mix of old and new data formats written at different times.</li> </ul> <p>In large systems, it is typical to roll out features in stages. This leads to situations where different versions of code must talk to each other.</p> <ul> <li><strong>Server-side rolling upgrades</strong>: The new version is rolled out to a few nodes at a time. After checking whether the new version is running smoothly, it is gradually rolled out to all the nodes.</li> <li><strong>Client-side updates</strong>: The client may not update their apps for weeks or months after the schema change.</li> </ul> <p>Because multiple versions of the code and data coexist, it requires maintaining compatibility in two directions:</p> <ul> <li><strong>Backwards compatibility</strong>: Newer code can read data written by older code. This is simpler since new code is written with knowledge of the old format.</li> <li><strong>Forwards compatibility</strong>: Older code can read data written by newer code. This is more difficult because it requires the old code to gracefully ignore additions made by a version of the code that didn’t exist when it was written.</li> </ul> <h2 id="encoding-data">Encoding Data</h2> <p>Programs typically work with data in two distinct forms:</p> <ul> <li><strong>In-memory</strong>: Data is kept in objects, structs, lists, arrays, etc. These are optimized for efficient access by the CPU using pointers.</li> <li><strong>Byte sequences</strong>: When writing to a file or sending data over a network, data must be encoded as a self-contained sequence of bytes. The original pointers only make sense within a specific process and are not useful to other processes.</li> </ul> <p>The translation from the in-memory to the byte sequence representation is called <strong>encoding</strong>, and the reverse is <strong>decoding</strong>.</p> <h3 id="language-specific-formats">Language-Specific Formats</h3> <p>Most modern languages have built-in encoding libraries (<code class="language-plaintext highlighter-rouge">java.io.Serializable</code>, <code class="language-plaintext highlighter-rouge">pickle</code> in Python, <code class="language-plaintext highlighter-rouge">Marshal</code> in Ruby). These are convenient, but it is a bad idea to use them in large applications:</p> <p><strong>1. Language lock-in</strong>: The encoding is often tied to a specific language, making it difficult to integrate with systems written in other languages.</p> <p><strong>2. Security</strong>: To restore data into objects, the decoder must be able to instantiate arbitrary classes. This is a classic security problem because attackers can potentially instantiate dangerous classes and achieve <strong>remote code execution</strong>.</p> <p><strong>3. Neglected Versioning</strong>: Forwards and backwards compatibility is typically not considered in these libraries.</p> <p><strong>4. Efficiency</strong>: These formats are often slow and produce bloated, large byte sequences (Java’s built-in serialization is a classic example).</p> <h3 id="textual-formats-json-xml-csv">Textual Formats: JSON, XML, CSV</h3> <p>JSON, XML, and CSV are standardized, human-readable encodings that are widely supported. However, they have subtle flaws:</p> <p><strong>1. Numerical Ambiguity</strong>: XML and CSV cannot distinguish between a number and a string of digits without an external schema.</p> <ul> <li>JSON distinguishes numbers from strings but does not distinguish integers from floating-point numbers and lacks a specified precision.</li> </ul> <p><strong>2. Binary Strings</strong>: JSON and XML support Unicode text well but do not natively support binary strings (sequences of bytes).</p> <ul> <li>Developers work around this by using <strong>Base64 encoding</strong>, which increases the data size by 33%.</li> </ul> <p><strong>3. Schema Complexity</strong>: While JSON and XML have optional schema support, they are very complicated. Most applications hardcode the logic for interpreting data types correctly instead.</p> <ul> <li>CSV doesn’t have any schema, leaving it up to the application to define row and column meanings. This can lead to some inconsistent parsing; e.g. what if the values contain commas or newlines?</li> </ul> <p>For cases where human readability is less important than performance, there are binary encoding variants for the textual formats:</p> <ul> <li>JSON: MessagePack, BSON, BJSON, UBJSON, BISON, etc</li> <li>XML: WBXML, Fast Infoset</li> </ul> <p>However, most of these formats are schemaless, which forces them to store every field name alongside the data values in the encoded byte sequence. This overhead significantly restricts how much you can actually compress the data, often resulting in only a small space reduction compared to the original text.</p> <h3 id="thrift--protocol-buffers">Thrift &amp; Protocol Buffers</h3> <p>Apache Thrift and Protocol Buffers (protobuf) are binary encoding libraries that use a schema to define the data structure before any encoding happens.</p> <div class="language-protobuf highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">message</span> <span class="nc">Person</span> <span class="p">{</span>
  <span class="k">required</span> <span class="kt">string</span> <span class="na">user_name</span>       <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="k">optional</span> <span class="kt">int64</span>  <span class="na">favorite_number</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="k">repeated</span> <span class="kt">string</span> <span class="na">interests</span>       <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>After defining the schema, the protobuf compiler will use the schema to generate new source files in the target language. These files contain methods that know how to turn an object into the correct binary sequence.</p> <p>One of the major benefits of having a schema is that it doesn’t require storing the field name alongside the data values. Instead, the schema contains <strong>field tags</strong> that act as a compact alias for the fields.</p> <p>However, the downside is that there are specific rules for how the datamodel can change over time.</p> <ul> <li>You can add new fields as long as they have new tag numbers. Old code will ignore tags it doesn’t recognize (forwards compatibility).</li> <li>New code can read old data as long as the existing tag numbers do not change. However, any new field you add must be optional or have a default value. <ul> <li>If not, then new code would fail when trying to read old data that lacks the field.</li> </ul> </li> <li>You can only remove optional fields, and you must never use that specific tag number again to avoid confusing newer code.</li> </ul> <h3 id="avro">Avro</h3> <p>Apache Avro is another binary encoding format that is significantly different from protobuf and Thrift. It also uses a schema to specify the structure of the data being encoded, but it has <strong>no tag numbers</strong>.</p> <ul> <li>The benefit of this is that the Avro binary encoding is the most compact out of the other encodings discussed.</li> <li>This is also much more friendly to dynamically generated schemas, since it doesn’t require manually updating the field tags (like in protobuf).</li> <li>As a result, the reader must go through the fields in the exact order they appear in the schema.</li> </ul> <p>Because of this, it requires thinking more carefully about schema evolution.</p> <ul> <li>The <strong>writer’s schema</strong> is the schema the application used when it encoded the data.</li> <li>The <strong>reader’s schema</strong> is the one the application expects when it decodes the data.</li> </ul> <p>The schemas don’t have to be the same: they only need to be <strong>compatible</strong>. At that point, the Avro reader will read both schemas side by side and resolve the differences.</p> <ul> <li>Writer and reader can have schemas in different orders, since schema resolution will resolve the fields by field names.</li> <li>If a field appears in the writer’s schema but not the reader’s schema, it is ignored. If the code reading the data expects some field, but the writer’s schema does not contain a field of that name, it is filled in with a <strong>default value</strong> (declared in the reader’s schema).</li> </ul> <p>There are a few rules to maintain forwards and backwards compatibility:</p> <ul> <li>You can only add or remove a field that has a default value.</li> <li>Changing a datatype is possible if the reader can resolve the writer’s type into its own. This is governed by <strong>type promotion</strong>; e.g. widening from <code class="language-plaintext highlighter-rouge">int</code> to <code class="language-plaintext highlighter-rouge">long</code>. <ul> <li>This is backwards compatible (<code class="language-plaintext highlighter-rouge">int</code> in the old writer version can be converted to <code class="language-plaintext highlighter-rouge">long</code> in the new reader version), but not forwards compatible (<code class="language-plaintext highlighter-rouge">long</code> in the new writer version can’t be safely casted to <code class="language-plaintext highlighter-rouge">int</code> in the old reader version).</li> </ul> </li> <li>Changing a field name is possible but requires an alias. <ul> <li>For example, if we rename a field from <code class="language-plaintext highlighter-rouge">loginID</code> to <code class="language-plaintext highlighter-rouge">username</code>, we can redefine the field as <code class="language-plaintext highlighter-rouge">username</code> but add an alias of <code class="language-plaintext highlighter-rouge">loginID</code>.</li> <li>This is backwards compatible: the reader schema looks for <code class="language-plaintext highlighter-rouge">username</code>, sees that it is not present in the writer schema, then looks at <code class="language-plaintext highlighter-rouge">loginID</code> to decode the data.</li> <li>This is not forwards compatible: the reader has no knowledge of <code class="language-plaintext highlighter-rouge">username</code> or the alias. The writer schema will contain <code class="language-plaintext highlighter-rouge">username</code>, it will not know what to do and fail (unless there is a default).</li> </ul> </li> <li>Adding a branch to a <strong>union type</strong> is also backwards compatible but not forwards compatible. This is required if you want to allow a field to be <code class="language-plaintext highlighter-rouge">null</code> since that is not an acceptable default in Avro.</li> </ul> <p>Practically, we need an approach to send the writer’s schema to the reader. It is not an option to send the full schema with every record since this would ruin the space efficiency of the binary encoding.</p> <p>Depending on how the data is being moved, there are a few approaches:</p> <ol> <li><strong>Large files (object container files)</strong>: The writer includes the full writer’s schema once in a header at the beginning of a file. <ul> <li>The file becomes self-describing and the overhead is negligible when spread across millions of records.</li> </ul> </li> <li><strong>Databases with individual records</strong>: Because the records are being written at different times by different versions, they might use entirely different schemas. <ul> <li>The writer prepends a small version number or schema hash to every record.</li> <li>Using the version number, the reader looks up the corresponding schema in a central <strong>Schema Registry</strong> to decode the record.</li> </ul> </li> <li><strong>Network connections (RPC)</strong>: During the initial connection handshake, the two processes negotiate which schema they will use.</li> </ol> <h2 id="dataflow-models">Dataflow Models</h2> <p>We talked about ways to encode/decode data but we haven’t discussed how the data actually flows from one process to another. This is a <em>dataflow model</em>.</p> <h3 id="dataflow-through-databases">Dataflow through Databases</h3> <p>A sender writes to the database, and the receiver reads from the database later.</p> <p>During a rolling upgrade, or an environment where multiple versions of the service access the same database, we will have newer code and older code running in parallel. These can lead to the following <strong>read-modify-write trap</strong>:</p> <ol> <li>A newer version of the code adds a field and writes it to the database.</li> <li>An older version of the code reads that record.</li> <li>The older code decodes the record into an in-memory object, modifies a different field, and writes the object back to the database.</li> <li>Because the older code’s model didn’t include the new field, it is often lost during the re-encoding process.</li> </ol> <p>To avoid this, databases require both directions of compatibility:</p> <ul> <li><strong>Backwards compatibility</strong>: Current code must be able to read data written by previous versions of the code.</li> <li><strong>Forward compatibility</strong>: A record written by a new version of the code might be updated by an older version still running in the cluster.</li> <li>Encoding formats (like Avro, Protobuf) support preserving unknown fields, but the application code must be carefully designed to keep those unknown fields intact during a read-modify-write cycle.</li> </ul> <h3 id="dataflow-through-services-rest-and-rpc">Dataflow through Services: REST and RPC</h3> <p>When processes need to communicate over a network, the most common arrangement is the <strong>client-server model</strong>. In this setup, servers expose an API (the service), and clients connect to perform requests.</p> <h4 id="web-services">Web Services</h4> <p>When HTTP is the underlying protocol, it is called a <strong>web service</strong>. There are two styles:</p> <ul> <li><strong>REST (Representational State Transfer)</strong>: A design philosophy, not a protocol, that uses standard HTTP features like URLs, cache control, and authentication. It uses simple formats like JSON.</li> <li><strong>SOAP</strong>: An XML-based protocol that aims to be independent of HTTP. It uses WSDL (Web Services Description Language) for code generation, which is useful in statically typed languages but complex to manage.</li> </ul> <h4 id="flaws-with-the-rpc-model">Flaws with the RPC model</h4> <p>RPC frameworks (gRPC, Thrift, Avro) attempt to make a remote network request look like a local function call within the same process (<strong>location transparency</strong>). However, this is fundamentally flawed, because network requests differ drastically from local calls:</p> <ul> <li>Unpredictability: A local call either succeeds or fails based on your parameters. Network requests can fail or be lost due to external factors outside your control.</li> <li>Timeouts: A network request may return without a result due to a timeout, leaving you unsure if the request actually reached the server or not.</li> <li>Retries: If you retry a failed request, you might perform the action twice (unless you build in <strong>idempotence</strong>)</li> <li>Latency: Local calls have predictable execution times, whereas network latency is wildly variable depending on congestion and server load.</li> <li>Parameters: You can pass references to local objects efficiently. For RPC, all data must be encoded into a sequence of bytes, which becomes problematic for large objects.</li> </ul> <h4 id="evolution">Evolution</h4> <p>For the system to evolve, clients and servers must be deployable independently.</p> <ul> <li>We usually assume that all servers will be updated first, and then all the clients.</li> <li>This means that we only need backwards compatibility on requests, and forward compatibility on responses.</li> <li>The provider of a service cannot force the users to upgrade, so compatibility needs to be maintained for a long time. <ul> <li>If a breaking change is unavoidable, service providers must maintain multiple versions of the API.</li> </ul> </li> </ul> <h3 id="message-passing-dataflow">Message-Passing Dataflow</h3> <p>The middleground between RPC and databases are <strong>asynchronous message-passing systems</strong>. Instead of a direct network connection, the sender transmits a message to a message broker.</p> <h4 id="message-brokers">Message Brokers</h4> <p>Using a broker (Kafka, ActiveMQ, NATS) offers several benefits over direct RPC:</p> <ul> <li><strong>Reliability through buffering</strong>: The broker acts as a buffer if the recipient is temporarily unavailable or overloaded, which prevents the sender from crashing or losing data.</li> <li><strong>Automatic redelivery</strong>: If a consumer process crashes, the broker can redeliver the message once the process restarts, preventing data loss.</li> <li><strong>Network decoupling</strong>: The sender doesn’t need to know the specific IP address or port of the recipient; it only needs to know the topic or queue name.</li> <li><strong>Fan-out capability</strong>: A single message can be published to a topic and consumed by multiple different services simultaneously.</li> <li><strong>Logical decoupling</strong>: The publisher simply sends the message and doesn’t need to care who is consuming it.</li> </ul> <h4 id="distributed-actor-frameworks">Distributed Actor Frameworks</h4> <p>The actor model is a programming model for concurrency in a single process. Instead of managing threads, locks, or shared memory, logic is divided into actors:</p> <ul> <li>State isolation: each actor maintains its own local state that is not shared with any other actor.</li> <li>Single-threaded processing: An actor processes only one message at a time, eliminating race conditions within the actor’s logic.</li> <li>Asynchronous messaging: Actors communicate exclusively by sending and receiving asynchronous messages.</li> </ul> <p>A distributed actor framework is a system that takes the actor model and extends it so that it works across multiple nodes in a network. In particular, a framework becomes distributed when it provides <strong>location transparency</strong>.</p> <ul> <li>The framework handles network complexity. When Actor A sends a message to Actor B, it uses the same code regardless of the location of Actor B.</li> <li>If the actor is remote, the framework transparently encodes the message into bytes, sends it over the wire, and decodes it on the other side.</li> <li>These models usually assume messages can be lost (even locally), and have strategies to handle resilience.</li> </ul> <p>For evolution, a distributed actor framework integrates a message broker and the programming language into one package. However, this comes with the same compatibility hurdles:</p> <ul> <li>If you perform a rolling upgrade, a node running new code might send a message to a node running old code.</li> <li>You must choose an encoding format that supports backward and forward compatibility, or your actors will crash when they receive a message from a different version.</li> </ul>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><summary type="html"><![CDATA[Data models (Avro/Protobuf), Schema Evolution, Dataflow Models]]></summary></entry><entry><title type="html">5. Forward/Future Prices</title><link href="https://vgraman0.github.io/blog/2026/hull-options-ch5/" rel="alternate" type="text/html" title="5. Forward/Future Prices"/><published>2026-01-29T12:00:00+00:00</published><updated>2026-01-29T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/hull-options-ch5</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/hull-options-ch5/"><![CDATA[<p>This chapter walks through the forward and future pricing formulas using arbitrage arguments.</p> <p>We will make the following assumptions:</p> <ol> <li>No transaction costs.</li> <li>Uniform tax rate on all net trading profits.</li> <li>Ability to borrow/lend at the same risk-free rate.</li> <li>Market participants take advantage of arbitrage opportunities as they occur.</li> </ol> <p>We will use the notation:</p> <ul> <li>$T$: Time until delivery date in a forward or futures contract.</li> <li>$S_0$: Price of the asset underlying the forward or futures contract today.</li> <li>$F_0$: Forward or futures price today.</li> <li>$r$: Zero-coupon risk-free rate using continuous compounding for a maturity of $T$.</li> </ul> <h2 id="forward-prices-for-an-investment-asset">Forward prices for an investment asset</h2> <h3 id="no-income-no-storage-costs">No income, no storage costs</h3> <p>Take a forward contract on an investment asset with price $S_0$ that provides no income and has no storage costs.</p> <p>Then,</p> \[F_0 = S_0 \exp(rT)\] <p>This is derived using an arbitrage argument: If $F_0 &gt; S_0\exp(rT)$:</p> <ol> <li>Borrow $S_0$ at the risk-free rate.</li> <li>Buy one share of the stock.</li> <li>Short a forward contract to sell the share at $F_0$ at $T$.</li> </ol> <p>At expiry, we make $F_0 - S_0\exp(rT) &gt; 0$ risk-free profit.</p> <p>If $F_0 &lt; S_0 \exp(rT):$</p> <ol> <li>Short the stock and immediately sell in the market for $S_0$.</li> <li>Invest $S_0$ at the risk-free rate.</li> <li>Buy a forward contract to buy the stock at $F_0$ at $T$.</li> </ol> <p>At expiry, we make $S_0\exp(rT) - F_0 &gt; 0$ risk-free profit.</p> <h3 id="known-income">Known income</h3> <p>Suppose an investment asset pays income with present value $I$. Then, for a forward contract on the asset,</p> \[F_0 = (S_0 - I) \exp(rT).\] <p>If $F_0 &gt; (S_0 - I) \exp(rT)$: An arbitrageur borrows $S_0$, buys the asset, and shorts the forward. They use the income $I$ to pay off part of the loan and the forward sale proceeds to pay the rest, keeping the surplus as profit.</p> <p>If $F_0 &lt; (S_0 - I)\exp(rT)$: An arbitrageur shorts the asset, invests the proceeds ($S_0$), and takes a long forward position. The use the investment growth to pay the required income to the lender and buy the asset back at the low price $F_0$, keeping the profit.</p> <h3 id="known-yield">Known yield</h3> <p>Suppose the asset provides a yield rather than a fixed dollar amount. This means the income is expressed as a percentage of the asset’s price at the time it is payed. We can also model this by thinking of the price as staying constant but owning more of the asset.</p> <p>Define $q$ as the average yield on an asset during the time $T$.</p> <ol> <li>Borrow $S_0$ to buy one unit of the asset today.</li> <li>Enter a forward contract to sell the final amount of the asset ($\exp(qT)$ units) at time $T$ for price $F_0$.</li> <li>At maturity, the one unit has grown to $\exp(qT)$ units. You sell the units under the forward contract, receiving $F_0\exp(qT)$.</li> <li>At maturity, you repay the loan, which has grown to $S_0\exp(rT)$.</li> </ol> <p>Setting these equal, we obtain the formula</p> \[F_0 = S_0 \exp((r-q)T).\] <h2 id="valuing-forward-contracts">Valuing Forward Contracts</h2> <p>First, we need to different between price and value.</p> <ul> <li><strong>Forward price</strong> ($F_0$) is the delivery price $K$ that would be set if you negotiated a brand-new contract today.</li> <li><strong>Value of the Contract</strong> ($f$): What the existing contract is worth right now.</li> </ul> <p>The value of the long forward contract is given by</p> \[f = (F_0 - K)\exp(-rT).\] <p>We can prove this as follows.</p> <ul> <li>Take a portfolio consisting of a forward contract to buy the underlying for $K$ at time $T$, and a forward contract to sell the asset at $F_0$ at time $T$.</li> <li>The payoff at time $T$ is $S_T - K$ for the first contract, and $F_0 - S_T$ for the second contract. So the total payoff is $F_0 - K$.</li> <li>Based on the risk-free argument, the portfolio is worth $(F_0 - K)\exp(-rT)$ today.</li> </ul> <p>Combining this with the formula $F_0 = S_0\exp(-rT)$, we obtain \(f = S_0 - K\exp(-rT).\)</p> <p>We can form similar equations in the cases where we have known income or an investment asset with a given yield.</p>]]></content><author><name></name></author><category term="hull-options"/><category term="options"/><category term="derivatives"/><category term="forward"/><category term="future"/><summary type="html"><![CDATA[This chapter walks through the forward and future pricing formulas using arbitrage arguments.]]></summary></entry><entry><title type="html">3. Storage and Retrieval</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch3/" rel="alternate" type="text/html" title="3. Storage and Retrieval"/><published>2026-01-24T12:00:00+00:00</published><updated>2026-01-24T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch3</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch3/"><![CDATA[<p>This chapter is a foundational deep dive into how databases actually handle data under the hood.</p> <h1 id="database-data-structures">Database Data Structures</h1> <p>We can implement a simple key-value store with two Bash functions:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>db_set<span class="o">()</span> <span class="o">{</span>
    <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">,</span><span class="nv">$2</span><span class="s2">"</span> <span class="o">&gt;&gt;</span> database
<span class="o">}</span>

db_get<span class="o">()</span> <span class="o">{</span>
    <span class="nb">grep</span> <span class="s2">"^</span><span class="nv">$1</span><span class="s2">,"</span> database | <span class="nb">sed</span> <span class="nt">-e</span> <span class="s2">"s/^1,//"</span> | <span class="nb">tail</span> <span class="nt">-n</span> 1
<span class="o">}</span>
</code></pre></div></div> <p>The underlying storage format is a text file where each line contains a key-value pair (comma-separated).</p> <ul> <li><code class="language-plaintext highlighter-rouge">db_set()</code> appends a new key-value pair to a text file</li> <li><code class="language-plaintext highlighter-rouge">db_get()</code> uses grep to find the last occurrence of the key in the file to return the latest value.</li> </ul> <p>The file that we use for storage and retrieval is a <strong>log</strong>, an append-only data file.</p> <p>This simple data structure comes with the trade-offs:</p> <ul> <li><strong>Performant writes</strong>: appending to the file is very efficient because it is a sequential operation.</li> <li><strong>Poor read performance</strong>: The cost of a lookup is $O(n)$, where $n$ is the number of entries.</li> </ul> <p>The solve this, we need an <strong>index</strong>. This is additional metadata that helps to locate the data. This improves read performance but incurs overhead since we need to write additional structures. Because of this, choosing an index is left to the application developer, using the knowledge of the typical query patterns.</p> <h3 id="hash-index">Hash Index</h3> <p>The simplest indexing strategy is to keep an <strong>in-memory hash map</strong> where every key is mapped to a <strong>byte offset</strong> in the data file.</p> <ul> <li>When writing a key, update the hash map with the offset of the data appended.</li> <li>To read, look up the offset in the hash map, seek to that location on disk, and read that value.</li> <li><strong>Constraint</strong>: All keys must fit in available RAM. This makes it perfect for workloads with many writes per key (like a counter), but a low number of distinct keys. This approach is used for <strong>Bitcask</strong> (the default storage engine for Riak).</li> </ul> <h3 id="compaction">Compaction</h3> <p>If we only ever append to a file, we might eventually run out of disk space. The solution is <strong>compaction</strong>:</p> <ol> <li>Break the log into segments.</li> <li>A background process throws away duplicate keys, keeping only the most recent update for each.</li> <li>Segments are merged together to keep the total number of files small.</li> </ol> <h3 id="practical-issues">Practical issues</h3> <p>While our key-value store was simple, there are a number of practical issues that need to be addressed for real database systems.</p> <ul> <li><strong>File format</strong>: CSV is easy for humans to read, but is inefficient for a database. <ul> <li>Use a binary format encoding the length of a string first, followed by raw bytes.</li> <li>This is faster for a machine to parse and simpler to process.</li> </ul> </li> <li><strong>Deleting records</strong>: In an append-only log, we cannot erase old lines. <ul> <li>Append a special record called a <strong>tombstone</strong> associated with the key.</li> <li>During compaction, it sees the tombstone and knows to discard any previous values for that key, eventually removing it from the new, merged segments.</li> </ul> </li> <li><strong>Crash recovery</strong>: Because the hash map lives in memory, a crash would wipe the index. <ul> <li><em>Naive solution</em>: rebuild the index by reading all the segments. For large files, the makes restarts very slow.</li> <li><em>Bitcask optimization</em>: Store a snapshot of each segment’s has map on disk. When the system reboots, it can load the snapshots into memory, which is faster than a full scan.</li> </ul> </li> <li><strong>Partially written records</strong>: A carsh can happen while the database is writing a record. <ul> <li>Detect the partial entries using checksums that are included in the file.</li> <li>When detecting a corrupted record, it can safely ignore that partial entry.</li> </ul> </li> <li><strong>Concurrency</strong>: handling multiple users at once is a classic database challenge. <ul> <li><em>Single writer</em>: many implementations use a single writer thread to avoid ace conditions.</li> <li><em>Multiple readers</em>: The data segments are immutable, so many threads can lead simultaneously without locking.</li> </ul> </li> </ul> <h3 id="tradeoffs">Tradeoffs</h3> <p>It might seem wasteful to never “overwrite” data in place, but it offers many advantages:</p> <ol> <li><em>Sequential writes</em>: Appending is much faster than random-access writes, especially on hard disks.</li> <li><em>Simpler recovery</em>: These are much simpler if segment files are immutable.</li> <li><em>No fragmentation</em>: Periodically merging segments prevents the data files from getting fragmented over time.</li> </ol> <p>There are still some limitations of the hash table index:</p> <ol> <li>The hash table must fit into memory, so it will not work for a massive number of keys. <ul> <li>Theoretically, we could use an on-disk hash map. But this would perform very poorly since it requires significant random access I/O.</li> </ul> </li> <li>Range queries are not efficient. It requires looking up each individual key in the range.</li> </ol> <h2 id="log-sorted-merge-trees-lsm-trees">Log-Sorted Merge-Trees (LSM-trees)</h2> <p><strong>Sorted String Tables (SSTables)</strong> address the limitations of our original log-structured storage by requiring that the sequence of key-value pairs in our segment files are <em>sorted by key</em>.</p> <ul> <li><strong>Efficient merging</strong>: Even if the segment files are larger than the available memory, we can merge them using a technique similar to merge sort. <ul> <li>Read multiple segments side-by-side, look at the first key in each, and copy the lowest key to a new output file (similar to <strong>mergesort</strong>). This produces a new merged segment file that is sorted by key.</li> </ul> </li> <li><strong>Sparse index</strong>: The in-memory index can be <strong>sparse</strong> since it only needs to store the offset for the start of each segment.</li> <li><strong>Block compression</strong>: Since search requires scanning within the segment range, we can group records into <strong>compressed blocks</strong> before writing them to disk. <ul> <li>The sparse index points to the start of these compressed chunks.</li> </ul> </li> <li><strong>Efficient range queries</strong>: Since the keys are physically adjacent on disk, range queries become a simple <strong>sequential read</strong>.</li> </ul> <h3 id="constructing-and-maintaining-sstables">Constructing and maintaining SSTables</h3> <p>Our incoming writes can occur in any order, so we need an additional structure to maintain the sort.</p> <ol> <li>When a write arrives, it is added to an in-memory balanced tree structure (like a red-black tree), called a <strong>memtable</strong>. <ul> <li>Once the memtable grows past a certain size threshold (a few megabytes), it is written to disk as an SSTable file.</li> <li>While the old memtable is being written to disk, a new memtable instance handles incoming writes.</li> </ul> </li> <li>To find a key, the storage engine <strong>first checks the memtable</strong> to see if the key is in-memory. Otherwise, it <strong>checks the SSTable segments</strong> (starting with the most recent segment, then the next-older segment, etc).</li> <li>A compaction process runs in the background to combine segment files and to discard overwritten or deleted values.</li> </ol> <p>One scenario we still need to handle is the case where the database crashes. In this case, everything in the memtable is wiped out since it hasn’t been written to an SSTable yet. To resolve this, we use an <strong>append-only log</strong> corresponding to each memtable. - Each write is appended to an unsorted log before being added to the memtable. - If the system crashes, the database reads to log to rebuild the memtable. - Once a memtable is flushed to an SSTable, the recovery log is deleted because the data has already been persisted.</p> <p>This whole scheme—memtable + recovery log + SSTables + background merging—is a <strong>Log-Structured Merge-Tree (LSM-Tree)</strong>.</p> <h3 id="bloom-filters">Bloom Filters</h3> <p>The biggest performance bottleneck in an LSM-tree occurs when you search for a key that does not exist in the database.</p> <ul> <li>Because the data is spread across the memtable and multiple SSTable segments, the engine has to check the memtable, and then every segment on disk before it can detect that the key is missing.</li> <li>This leads to many unnecessary and expensive disk reads.</li> </ul> <p>A <strong>Bloom filter</strong> is a memory-efficient data structure used to approximate the contents of a set.</p> <ul> <li>When a key is added to the database, it is “hashed” several times, and the corresponding bits in a bit array are set to $1$.</li> <li>If we check the filter for a key and any of the bits are $0$, the engine knows that the key is not in the database.</li> <li>If all the bits are $1$, the key is there with some probability. There is a change of a false positive, but the engine can fall back to a standard disk scan in those cases.</li> </ul> <h4 id="false-positive-rate">False positive rate</h4> <p>Suppose we have a bit array of size $m$ and $k$ independent hash functions.</p> <p>The probability a specific bit is still $0$ after $k$ hashes for $n$ elements is</p> \[\left(1 - \frac{1}{m} \right)^{kn}.\] <p>Using the identity $\lim_{x \to \infty} \left(1 - \frac{1}{x} \right)^x = \frac{1}{e}$, we can approximate</p> \[\left(1 - \frac{1}{m} \right)^{kn} = \left(1 - \frac{1}{m} \right)^{m \cdot \frac{kn}{m}} \approx e^{-kn/m}.\] <p>Thus, the probability a bit is $1$ is given by</p> \[1 - e^{-kn/m}.\] <p>Finally, the false positive rate $\epsilon$ is the probability that all $k$ bits for a new element are already set to $1$. This is given by</p> \[\epsilon = \left( 1 - \left(1 - \frac{1}{m} \right)^{kn}\right)^k\approx \left(1 - e^{-kn/m}\right)^k.\] <h2 id="b-trees">B-trees</h2> <p>While LSM-trees are gaining popularity, <strong>B-trees</strong> are the most widely used indexing structure and the standard implementation in almost all relational databases.</p> <p>The core difference between the two has to do with how they organize data on disk:</p> <ul> <li>LSM-trees break the database into variable-size segments (several MBs) and write them sequentially.</li> <li>B-trees break the database into fixed-size blocks/pages (4 KB in size).</li> </ul> <p>A B-tree lookup starts at a single <strong>root page</strong>.</p> <ul> <li>Each page contains keys and <strong>references to child pages</strong>. Each child is responsible for a continuous range of keys.</li> <li>The number of references in one page is the <strong>branching factor</strong>. In practice, this is typically several hundred.</li> <li>Because of the high branching factor, a massive database can be navigated in a tree only three or four levels deep. <ul> <li>A 4-level tree of 4 KB pages can store up to <strong>256 TB</strong>.</li> </ul> </li> </ul> <p>To update the value for an existing key in a $B$-tree:</p> <ul> <li>Search for the leaf page containing the key, change the value, and write that page back to disk.</li> <li>If a new key is added and there isn’t enough space in a page, it is <strong>split</strong> into two half-full pages.</li> <li>The parent page is then updated to account for the new subdivision of key ranges. This ensures that the tree remains balanced with a depth of $O(\log n)$.</li> </ul> <p>Because B-trees overwrite pages, a crash during a page split is dangerous—it can leave the index corrupted (an “orphan” page with no parent).</p> <ul> <li>To make the database resilient, B-trees use a <strong>write-ahead log (WAL)</strong>.</li> <li>This is an append-only file where every modification is written before it is applied to the tree pages.</li> <li>The database can use this log to restore the tree to a consistent state.</li> </ul> <p>Since multiple threads might access the tree simultaneously, B-trees protect their data structures with <strong>latches</strong> to provent a thread from seeing the tree in an inconsistent state during a split.</p> <h3 id="b-tree-optimizations">B-tree optimizations</h3> <p>B-trees have been the industry standard since the 1970s and engineers have developed several optimizations for extra performance and reliability.</p> <p><strong>Copy-on-Write (Shadow Paging)</strong>: Instead of overwriting a page in-place and relying on a WAL, some databases (e.g. LMDB) use copy-on-write:</p> <ul> <li>A modified page is written to a new location on disk.</li> <li>The database then creates new versions of parent pages that point to this new location, cascading up the tree.</li> <li>This is helpful for <strong>snapshot isolation</strong>: readers can continue looking at the old version of the tree while a writer is busy creating a new one.</li> </ul> <p><strong>Key Compression (Abbreviation)</strong>: Increase the efficiency of each page by reducing the space each key takes up.</p> <ul> <li>Interior pages don’t need the full key. They only need enough information to act as a boundary between pages.</li> <li>By packing more keys onto a single page, we increase the branching factor and reduce the depth.</li> </ul> <p><strong>Sequential Layout</strong>: In a standard B-tree, pages can be scattered anywhere on disk. This is inefficient for range scans because every new page might require a random disk seek.</p> <ul> <li>Many implementations try to lay out the tree so that leaf pages appear in sequential order on disk.</li> <li>Maintaining this order is very difficult as the tree grows and pages split.</li> <li>By contrast, LSM-trees have an advantage since the merging process naturally rewrites data in large, sequential segments.</li> </ul> <p><strong>Sibling pointers</strong>: To make range scans faster, many B-trees add extra pointers that aren’t part of the strict tree hierarchy.</p> <ul> <li>Each leaf page can contain a reference to its sibling pages to the left and right.</li> <li>This allows a query to scan across the leaves in order without having to jump bank up to the parent pages for every new range.</li> </ul> <h3 id="b-trees-vs-lsm-trees-architectural-trade-offs">B-Trees vs LSM-Trees: Architectural Trade-offs</h3> <p><strong>1. Write Path &amp; Throughput</strong></p> <ul> <li>LSM-trees turn <strong>random writes</strong> into <strong>sequential I/O</strong> by <strong>buffering</strong> in a memtable and flushing to disk periodically.</li> <li>B-trees require at least <strong>two disk writes</strong> for every transaction: once for the <strong>write-ahead log</strong> (WAL) and the writes for updating the index.</li> <li>B-trees have higher <strong>write amplification</strong> since they operate using a page layout. Updating 10 bytes still requires writing a <strong>full 4KB page</strong>. LSM-trees have lower amplification, but they pay the cost later during <strong>background compaction</strong>.</li> </ul> <p><strong>2. Read Path &amp; Predictability</strong></p> <ul> <li>B-trees have <strong>better read performance</strong> because every key lives in <strong>exactly one logical location</strong>. This leads to $O(\log n)$ lookup times.</li> <li>The keys are <strong>fragmented</strong> for LSM-trees, so it requires checking the memtable and then potentially <strong>many SSTable segments</strong> for a given key.</li> <li>LSM-trees can suffer from <strong>compaction interference</strong>. If the background compaction thread falls behind or saturdates disk I/O, it can cause <strong>sudden spikes in request latency</strong> that are hard to debug.</li> </ul> <p><strong>3. Storage Efficiency</strong></p> <ul> <li>B-trees suffer from <strong>internal fragmentation</strong> (unused space inside a page after a split) and <strong>external fragmentation</strong> (pages scattered across disk).</li> <li>LSM-trees are <strong>more compact</strong> because they don’t use fixed-size pages. By rewriting data into new segments, they effectively <strong>“defragment”</strong> the database constantly. Because the index is sorted, the SSTables are <strong>highly compressible</strong>.</li> </ul> <h2 id="other-indexing-structures">Other indexing structures</h2> <p><strong>1. Multi-index structures</strong></p> <ul> <li>While a primary key uniquely identifies a row, <strong>secondary indexes</strong> are non-unique. They are helpful for filtering by attributes (e.g. find all users in Mumbai).</li> <li>Databases handle non-unique keys by either storing a <strong>posting list</strong> (a list of row IDs for each key) or by appending the row ID to the key to force uniqueness.</li> <li>Every secondary index added to the table incurs additional <strong>write overhead</strong>, since inserting an entry requires updating the primary index and potentially $n$ secondary indexes.</li> </ul> <p><strong>2. Index Physical Layout</strong></p> <ul> <li><strong>Heap files (non-clustered)</strong>: The index stores a pointer to a location in a central “heap”. <ul> <li>This is efficient for updates that don’t change the key (only the heap is modified).</li> <li>It requires an <strong>extra hop</strong> to get to the actual data, which can hurt read performance.</li> </ul> </li> <li><strong>Clustered index</strong>: The row data lives inside the index (usually the primary key). <ul> <li><strong>Faster reads</strong> because there is no extra hop from the index to a separate heap file.</li> <li>Higher <strong>write overhead</strong> and increased <strong>storage requirements</strong> due to the data being embedded in the index.</li> </ul> </li> <li><strong>Covering index</strong>: A “lite” version of clustering that only stores a few extra columns inside the index. <ul> <li>If the query asks for columns that are already in the index, the database can <strong>answer the query without looking at the main table</strong>.</li> </ul> </li> </ul> <p><strong>Multi-column indexes</strong>: Some queries require multiple fields simultaneously.</p> <ul> <li><strong>Concatenated index</strong>: Combines fields into a single key. This is useful for queries matching the prefix of the index but useless for searching by the second column alone.</li> <li><strong>Multi-dimentional indexes</strong>: Essential for geospatial data. A standard B-tree cannot efficiently handle 2D range queries. <ul> <li>Specialized spatial indexes such as <strong>R-trees</strong> or <strong>Quadtrees</strong> are used to index areas rather than points.</li> </ul> </li> </ul> <h1 id="column-oriented-storage">Column-Oriented Storage</h1> <p>For analytic queries that only need 4 or 5 columns out of a 100-column wide table, a <strong>row-oriented</strong> engine must still load every row from disk into memory. The engine has to parse these wide rows and filter out the vast majority of attributes, which is a waste of I/O and time.</p> <p>Instead, <strong>column-oriented storage</strong> stores all values from each column together in separate files.</p> <ul> <li>A query only needs to read and parse specific columns required for that query.</li> <li>The database can reassemble a row $k$ by taking the $k$-th entry from each individual column file.</li> </ul> <h2 id="compression">Compression</h2> <p><strong>Bitmap encoding</strong>: when the number of distinct values in a column is small relative to the total number of rows, we can compress the values into a bitmap.</p> <p><img src="/assets/img/ddia/ch3-bitmap-encoding.png" alt="Bitmap encoding for column compression"/></p> <p>The database creates a separate bitmap for every distinct value in the column.</p> <ul> <li>Each bitmap has one bit for every row in the table.</li> <li>The bit is set to $1$ if the row contains the specific value, and $0$ otherwise.</li> </ul> <p>When the number of distinct values is very large, the bitmaps will be sparse. In that case, we can use <strong>run-length encoding</strong> which makes the representation of the binary value string more compact.</p> <h2 id="hardware-acceleration">Hardware Acceleration</h2> <p>Column-oriented storage has immediate benefits due to reading less data from disk (less I/O), but the columnar systems are also designed to be CPU-efficient.</p> <p><strong>1. Cache Locality</strong></p> <ul> <li>Because columnar storage stores <strong>uniform data types sequentially</strong>, a single 64-byte <strong>cache line</strong> fetch from RAM is packed with relevant data.</li> <li>Compression also allows the engine to fit <strong>10x-100x more useful rows</strong> into the <strong>L1/L2 cache</strong> than a row-oriented format.</li> </ul> <p><strong>2. SIMD</strong></p> <ul> <li>Modern CPUs feature <strong>SIMD instructions</strong> that allow a processor to perform a <strong>single operation on multiple data points</strong> simultaneously.</li> <li>Because a column is a contiguous array of the same type, the CPU can load a vector of values into a <strong>wide register</strong> and process them in a <strong>single clock cycle</strong>.</li> <li>In a row-oriented system, SIMD can’t be used as easily because the column of interest is <strong>interrupted by other irrelevant columns</strong>.</li> </ul> <p><strong>3. Vectorization</strong> Columnar engines typically process <strong>large vectors of data</strong> (typically 1k-4k rows) at once.</p> <ul> <li>In a row-oriented system, processing a single row requires more complex code: checking data types, handling null data, function calls. In a <strong>vectorized loop</strong>, these checks are done <strong>once for the entire vector</strong>, drastically reducing the number of CPU cycles spent on bookkeeping.</li> <li>CPUs use <strong>branch prediction</strong> to guess the path of if/else statements. Row-oriented logic is full of <strong>unpredictable branches</strong>, which leads to <strong>pipeline bubbles</strong> when the CPU guesses wrong. Vectorized <strong>tight loops</strong> are perfectly predictable, allowing the CPU to <strong>saturate its execution pipeline</strong>.</li> </ul>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><category term="storage"/><category term="retrieval"/><summary type="html"><![CDATA[Indexes, B-trees vs LSM-trees, Column-Oriented Storage]]></summary></entry><entry><title type="html">4. Interest Rates</title><link href="https://vgraman0.github.io/blog/2026/hull-options-ch4/" rel="alternate" type="text/html" title="4. Interest Rates"/><published>2026-01-20T12:00:00+00:00</published><updated>2026-01-20T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/hull-options-ch4</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/hull-options-ch4/"><![CDATA[<h2 id="types-of-rates">Types of Rates</h2> <p><strong>Treasury rates</strong>: Rates on T-bills and T-bonds, considered risk-free.</p> <p><strong>Overnight rates</strong>: Fed funds rate in the US (other countries have similar systems). Arises from reserve requirements—banks with excess reserves lend overnight to banks with shortfalls.</p> <p><strong>Repo rate</strong>: Interest rate on a repurchase agreement, where securities are sold with an agreement to buy them back later. Effectively a collateralized loan.</p> <h2 id="reference-rates">Reference Rates</h2> <p><strong>LIBOR</strong> (London Interbank Offered Rate): Banks submitted estimates of their borrowing rates. Phased out due to manipulation scandals and lack of underlying transactions.</p> <p><strong>SOFR</strong> (Secured Overnight Financing Rate): The replacement for USD LIBOR. A volume-weighted median of rates on overnight Treasury repo transactions—based on actual trades rather than estimates.</p> <h2 id="bond-pricing">Bond pricing</h2> <p>A bond is a series of future cash flows: periodic coupons and a final principal (face value).</p> <p>Suppose the coupon $c_i$ payments are at times $t_i$, $P$ is the principal, and $r_i$ is the zero-rate for maturity $t_i$. The formula for the bond price is given as</p> \[P_{\text{bond}} = \sum_{i=1}^n c_i e^{-r_i t_i} + Pe^{-r_nt_n}.\] <p>This comes from discounting the values of the future cash flows to their corresponding present value.</p> <h3 id="bond-yield">Bond yield</h3> <p>The yield is a single “average” interest rate such that after being applied to each of the bond cash flows, the present value would equal the market price.</p> <p>In other words, we would solve the equation</p> \[P_{\text{bond}}(y) = \sum_{i=1}^n c_i e^{-y t_i} + Pe^{-yt_n} = P_{\text{market}}\] <p>This is usually calculated through an iterative method, such as Newton-Raphson.</p> <h3 id="par-yield">Par yield</h3> <p>The par yield is the coupon rate (per annum) that causes the bond price to equal its principal.</p> <p>For a bond that pays semi-annual coupons, we would solve</p> \[P_{\text{bond}}(c) = \frac{c}{2}\sum_{i=1}^n e^{-y t_i} + Pe^{-yt_n} = P\] <p>Rearranging the equation gives</p> \[c = 2P\left (\frac{1 - e^{-yt_n}}{\sum_{i=1}^n e^{-yt_i}}\right)\] <p>This is usually written as</p> \[c = \frac{P(1-d)m}{A},\] <p>where $A$ is the value of the annuity that pays one dollar on each coupon payment date, $m$ is the number of coupon payments per year, and $d$ is the present value of one dollar received at the maturity of the bond.</p> <h3 id="sensitivity">Sensitivity</h3> <p>When interest rates change $y$, the corresponding bond price will also change.</p> <p>To model this, we first approximate the change in a bond’s price given a change in yield $\Delta y$ as</p> \[\Delta P \approx \frac{dP}{dy} \Delta y + \frac{1}{2} \frac{d^2 P}{dy^2} (\Delta y)^2.\] <p>Normalizing by the bond price, we obtain</p> \[\frac{\Delta P}{P} = -D \Delta y + \frac{C}{2}\left( \Delta y\right)^2\] <p>where</p> \[D = -\frac{1}{P} \frac{dP}{dy}\] <p>and</p> \[C = \frac{1}{P} \frac{d^2P}{dy^2}\] <p>are the duration and convexity respectively.</p> <h2 id="forward-rates">Forward rates</h2> <p>A <strong>Forward Rate</strong> is the interest rate for a future period of time that is implied by the interest rates we see in the market today.</p> <p>Suppose $R_1$ and $R_2$ are the zero rates for maturities $T_1$ and $T_2$ respectively (where $T_2$ &gt; $T_1$).</p> <p>The forward rate $R_F$ is the rate we need to invest between $T_1$ and $T_2$ so that investing in at $R_2$ for $T_2$ years is equivalent to investing at $R_1$ for $T_1$ years and investing at $R_F$ between $T_1$ and $T_2$.</p> <p>Written out explicitly,</p> \[\begin{align*} e^{R_2T_2} &amp;= e^{R_1T_1} \cdot e^{R_F(T_2 - T_1)} \\ &amp;= e^{R_1T_1 + R_F(T_2 - T_1)} \end{align*}\] <p>Because $e^x$ is a monotonic function, we can apply the inverse to both sides to obtain</p> \[R_2T_2 = R_1T_1 + R_F(T_2 - T_1).\] <p>Rearranging and solving for $R_F$ gives</p> \[R_F = \frac{R_2T_2 - R_1T_1}{T_2 - T_1}.\] <h3 id="instantaneous-rates">Instantaneous rates</h3> <p>The <strong>instantaneous forward rate</strong> $f(T)$ represents the interest rate at a specific instant in time, i.e. the percentage growth rate of the investment at time $T$.</p> <p>Suppose $V(T)$ is the value of one dollar at time $T$. Then, \(f(T) := \frac{V'(T)}{V(T)}.\)</p> <p>We know that $V(T) = e^{R(T)T}$, where $R(T)$ is the zero rate at time $T$.</p> \[\begin{align*} V'(T) &amp;= e^{R(T)T} \frac{d}{dT} \left[R(T) T\right] \\ &amp;=- V(T) \frac{d}{dT}\log V(T). \end{align*}\] <p>This gives the expression</p> \[f(T) = -\frac{\partial }{\partial t} \log V(t).\] <p>Using the fundamental theorem of calculus,</p> \[\int_0^T f(t)\, dt = -\int_0^T \frac{\partial}{\partial t} \log V(t)\,dt = - \left(\log V(T) - \log V(0)\right).\] <p>We know $V(0) = 1$, so</p> \[\int_0^T f(t)\,dt = - \log V(T) = R(T)T.\] <p>Rearranging the expression, we obtain</p> \[R(T) = \frac{1}{T} \int_0^T f(t)\,dt.\] <p>In other words, the zero rate is the time-weighted average of the instantaneous forward rates between today and maturity.</p>]]></content><author><name></name></author><category term="hull-options"/><category term="options"/><category term="derivatives"/><category term="interest-rates"/><summary type="html"><![CDATA[Technical overview of rate types (SOFR/LIBOR), bond pricing, yield sensitivity (Duration/Convexity) and forward rates.]]></summary></entry><entry><title type="html">2. Data Models and Query Languages</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch2/" rel="alternate" type="text/html" title="2. Data Models and Query Languages"/><published>2026-01-16T12:00:00+00:00</published><updated>2026-01-16T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch2</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch2/"><![CDATA[<p>Data models are perhaps the most important part of developing software. They shape not only how we write code, but how we <em>think</em> about the problem we’re solving. Most applications are built by layering data models: application objects map to database tables, which map to bytes in memory, which map to electrical currents. Each layer hides the complexity below it by providing a clean abstraction.</p> <h2 id="relational-model-vs-document-model">Relational Model vs Document Model</h2> <p>The <strong>relational model</strong> organizes data into relations (called tables in SQL), where each relation is an unordered collection of tuples (rows). Proposed by Edgar Codd in 1970, it became the dominant model for structured data:</p> <ul> <li><strong>Transaction processing</strong>: Banking, airline reservations, stock trading</li> <li><strong>Batch processing</strong>: Payroll, reporting, analytics</li> </ul> <p>Relational databases generalized well beyond their original business data processing use case, and remained dominant for decades.</p> <h3 id="the-rise-of-nosql">The Rise of NoSQL</h3> <p>NoSQL databases emerged in the 2010s with several driving forces:</p> <ul> <li><strong>Scalability</strong>: Better handling of very large datasets or high write throughput than relational databases could offer</li> <li><strong>Specialized queries</strong>: Support for operations not well-served by the relational model</li> <li><strong>Schema flexibility</strong>: A more dynamic and expressive data model than rigid SQL schemas</li> <li><strong>Open source</strong>: Preference for free software over commercial database products</li> </ul> <p>The name “NoSQL” is somewhat of a misnomer—it’s better understood as “Not Only SQL.” Many applications now use a <strong>polyglot persistence</strong> approach, choosing the best tool for each use case.</p> <h3 id="the-object-relational-mismatch">The Object-Relational Mismatch</h3> <p>Most application code today is written in object-oriented languages. When data lives in relational tables, there’s an awkward translation layer between objects and rows/columns.</p> <blockquote> <p><strong>Impedance mismatch</strong>: The disconnect between the object-oriented programming model and the relational data model.</p> </blockquote> <p>Object-relational mapping (ORM) frameworks like ActiveRecord and Hibernate reduce boilerplate, but can’t completely hide the differences.</p> <h3 id="one-to-many-relationships">One-to-Many Relationships</h3> <p>Consider a LinkedIn profile: one person has multiple jobs, multiple education entries, and multiple contact methods. In a relational database, this requires <strong>normalization</strong>—splitting the data across multiple tables linked by foreign keys:</p> <p><img src="/assets/img/ddia/ch2-linkedin-relational.png" alt="Representing a LinkedIn profile in a relational schema requires multiple tables"/></p> <p>Document databases like MongoDB handle this more naturally. A JSON document can represent the entire profile in one place:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"user_id"</span><span class="p">:</span><span class="w"> </span><span class="mi">251</span><span class="p">,</span><span class="w">
  </span><span class="nl">"first_name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Bill"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"last_name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Gates"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"positions"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"job_title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Co-chair"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"organization"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Bill &amp; Melinda Gates Foundation"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"job_title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Co-founder"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"organization"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Microsoft"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"education"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"school_name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Harvard University"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"start"</span><span class="p">:</span><span class="w"> </span><span class="mi">1973</span><span class="p">,</span><span class="w">
      </span><span class="nl">"end"</span><span class="p">:</span><span class="w"> </span><span class="mi">1975</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <blockquote> <p><strong>Data locality</strong>: Document models keep related information together, enabling retrieval with a single query rather than multiple joins.</p> </blockquote> <h3 id="many-to-one-and-many-to-many-relationships">Many-to-One and Many-to-Many Relationships</h3> <p>Document models excel at one-to-many relationships but struggle with many-to-one and many-to-many relationships. If multiple people work at the same organization, that organization should be stored once and referenced—but document databases don’t traditionally support joins.</p> <p>This isn’t a new tension. The <strong>hierarchical model</strong> (IBM’s IMS, 1960s) also represented data as nested records and faced the same limitation.</p> <p>The <strong>network model</strong> (CODASYL) generalized hierarchical data by allowing records to have multiple parents:</p> <ul> <li>Records were linked like pointers in a programming language</li> <li>Accessing data required traversing an <strong>access path</strong> from a root record</li> <li>Application developers had to manage these traversal algorithms manually, making the code fragile and difficult to change</li> </ul> <p>The relational model solved this by letting the query optimizer choose the access path automatically—a key reason for its success.</p> <h3 id="schema-flexibility">Schema Flexibility</h3> <blockquote> <p><strong>Schema-on-read</strong>: The structure of the data is implicit and only interpreted when data is read. (Document databases)</p> <p><strong>Schema-on-write</strong>: The schema is explicit and the database enforces that all data conforms to it. (Relational databases)</p> </blockquote> <p>Schema-on-read is similar to dynamic typing in programming languages; schema-on-write is like static typing. Neither is universally better—the choice depends on the application.</p> <p>Schema-on-read is advantageous when:</p> <ul> <li>Items in a collection don’t all have the same structure</li> <li>The structure is determined by external systems you don’t control</li> <li>The schema changes frequently</li> </ul> <h3 id="convergence">Convergence</h3> <p>The distinction between relational and document databases is blurring:</p> <ul> <li>Most relational databases now support JSON documents and XML</li> <li>Some document databases support relational-like joins (RethinkDB)</li> <li>MongoDB drivers can automatically resolve document references (client-side joins)</li> </ul> <h2 id="graph-like-data-models">Graph-Like Data Models</h2> <p>When many-to-many relationships are common in your data, the graph model becomes natural. Graphs consist of <strong>vertices</strong> (nodes) and <strong>edges</strong> (relationships), and can model diverse data elegantly:</p> <p><img src="/assets/img/ddia/ch2-graph-model.png" alt="Graph data model representing people and locations"/></p> <p>Graphs aren’t limited to homogeneous data. Facebook’s social graph includes people, locations, events, checkins, and comments—all as vertices with different types of edges connecting them.</p> <h3 id="property-graphs">Property Graphs</h3> <p>In the <strong>property graph</strong> model (used by Neo4j, Titan, InfiniteGraph), each vertex has:</p> <ul> <li>A unique identifier</li> <li>Outgoing edges</li> <li>Incoming edges</li> <li>Properties (key-value pairs)</li> </ul> <p>Each edge has:</p> <ul> <li>A unique identifier</li> <li>Tail vertex (where the edge starts)</li> <li>Head vertex (where the edge ends)</li> <li>A label describing the relationship</li> <li>Properties (key-value pairs)</li> </ul> <p>You could model this in a relational database with two tables (vertices and edges), but graph queries become unwieldy. Finding all vertices within two hops requires two joins; within three hops requires three joins. Since the number of joins isn’t known in advance, you’d need recursive SQL queries.</p> <p><strong>Cypher</strong> is a declarative query language designed for property graphs (created for Neo4j). Finding everyone who emigrated from the US to Europe:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MATCH
(person) -[:BORN_IN]-&gt;  () -[:WITHIN*0..]-&gt; (us:Location {name:'United States'}),
(person) -[:LIVES_IN]-&gt; () -[:WITHIN*0..]-&gt; (eu:Location {name:'Europe'})
RETURN person.name
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">WITHIN*0..</code> syntax elegantly handles variable-length paths through the location hierarchy.</p> <h3 id="triple-stores">Triple-Stores</h3> <p>The <strong>triple-store</strong> model represents all information as three-part statements: <em>(subject, predicate, object)</em>.</p> <ul> <li>The <strong>subject</strong> is equivalent to a vertex</li> <li>The <strong>predicate</strong> and <strong>object</strong> are either: <ul> <li>A property: <code class="language-plaintext highlighter-rouge">(lucy, age, 33)</code> — Lucy is 33 years old</li> <li>A relationship: <code class="language-plaintext highlighter-rouge">(lucy, marriedTo, alain)</code> — Lucy is married to Alain</li> </ul> </li> </ul> <p>Triple-stores are closely related to the <strong>semantic web</strong> vision of machine-readable data on the internet, though that vision hasn’t fully materialized.</p> <p><strong>SPARQL</strong> is the query language for triple-stores, with syntax inspired by Cypher (or perhaps it’s the other way around—SPARQL predates Cypher).</p>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><category term="data"/><category term="query"/><summary type="html"><![CDATA[Comparing relational, document, and graph data models—and the query languages that accompany them]]></summary></entry><entry><title type="html">1. Reliable, Scalable, and Maintainable Applications</title><link href="https://vgraman0.github.io/blog/2026/ddia-ch1/" rel="alternate" type="text/html" title="1. Reliable, Scalable, and Maintainable Applications"/><published>2026-01-15T12:00:00+00:00</published><updated>2026-01-15T12:00:00+00:00</updated><id>https://vgraman0.github.io/blog/2026/ddia-ch1</id><content type="html" xml:base="https://vgraman0.github.io/blog/2026/ddia-ch1/"><![CDATA[<p>Many applications today are <strong>data-intensive</strong> rather than compute-intensive. The bottleneck is usually the amount of data, the complexity of data, and how fast it changes—not raw CPU power. This chapter establishes three fundamental concerns for building data systems.</p> <h2 id="thinking-about-data-systems">Thinking About Data Systems</h2> <p>Databases, caches, and queues are traditionally thought of as different categories of tools with different access patterns, performance characteristics, and implementations. But the boundaries are blurring:</p> <ul> <li><strong>Redis</strong>: A datastore that can also be used as a message queue</li> <li><strong>Kafka</strong>: A message queue with database-like durability guarantees</li> </ul> <p>Modern applications are usually too complex to solve with a single tool, requiring composite systems. In this view, the application developer becomes a <strong>data system designer</strong>, responsible for a system that provides specific guarantees to the client.</p> <p>The book focuses on three main concerns:</p> <ol> <li><strong>Reliability</strong>: Continuing to work correctly when things go wrong</li> <li><strong>Scalability</strong>: Having strategies to handle growth in load</li> <li><strong>Maintainability</strong>: Making it easy for people to work on the system over time</li> </ol> <h2 id="reliability">Reliability</h2> <p>First, distinguish between faults and failures:</p> <blockquote> <p><strong>Fault</strong>: When one component of the system deviates from its specification.</p> <p><strong>Failure</strong>: When the system as a whole stops providing the required service to the user.</p> </blockquote> <p>It’s impossible to reduce the probability of a fault to zero. The goal is to design <strong>fault-tolerant</strong> mechanisms that prevent faults from causing failures.</p> <p><strong>Types of faults:</strong></p> <ol> <li><strong>Hardware faults</strong>: Hard disks crashing, RAM failing, power outages. Usually handled with redundancy (RAID, backup generators).</li> <li><strong>Software errors</strong>: Systematic bugs in the system. Hard to anticipate and tend to cause more failures since they are correlated across nodes.</li> <li><strong>Human error</strong>: Configuration mistakes by operators—the leading cause of outages. <ul> <li>Design systems that minimize opportunities for error</li> <li>Provide sandbox environments to explore and experiment safely</li> </ul> </li> </ol> <h3 id="example-netflix-chaos-monkey">Example: Netflix Chaos Monkey</h3> <p>Netflix’s Chaos Monkey deliberately kills random server processes in production. The goal is to ensure that fault-tolerance mechanisms are actually working—not just theoretically sound.</p> <h2 id="scalability">Scalability</h2> <p>Scalability isn’t a simple “yes” or “no” label. It’s about how the system handles growth in specific circumstances.</p> <h3 id="describing-load">Describing Load</h3> <p>Choose <strong>load parameters</strong>—the dimensions of growth you want your system to handle:</p> <ul> <li>Requests per second</li> <li>Ratio of reads to writes</li> <li>Number of simultaneous users</li> </ul> <p><strong>Twitter case study:</strong></p> <ul> <li>Post tweet: avg 4.6k rps, peak 12k rps</li> <li>View timeline: 300k rps</li> </ul> <p>Handling 12k writes/sec is easy. But 300k reads/sec is harder—and Twitter guarantees tweet delivery to followers within 5 seconds.</p> <p><strong>Approach 1: Read-time joins</strong> Store tweets in a global table. On timeline request, look up everyone you follow, grab their tweets, and merge them. This is slow at 300k reads/sec.</p> <p><strong>Approach 2: Write-time fan-out</strong> Maintain a timeline cache per user. When someone tweets, insert it into every follower’s cache. Reading is fast since the work is pre-computed. But posting is expensive: with 75 followers on average, 4.6k tweets/sec becomes 345k cache writes/sec. This doesn’t scale for celebrities with millions of followers.</p> <p><strong>Twitter’s hybrid solution:</strong> Most users use write fan-out, but celebrity tweets are fetched separately and merged at read time.</p> <h3 id="measuring-performance">Measuring Performance</h3> <p>Different systems have different metrics:</p> <ul> <li><strong>Batch systems</strong> (Hadoop): Throughput (records/sec) or total processing time</li> <li><strong>Online systems</strong>: Response time—measured as a distribution, not a single number</li> </ul> <blockquote> <p><strong>Response time</strong> = time between client sending request and receiving response</p> <p><strong>Latency</strong> = time a request spends waiting to be handled</p> </blockquote> <p><strong>Percentiles matter more than averages.</strong> Amazon optimizes for p99.9 because customers with the most data (from many purchases) tend to have the slowest requests—and they’re the most valuable.</p> <p><strong>Tail latency amplification</strong>: In systems with multiple backend calls, a single slow sub-request can slow down the entire user response.</p> <h3 id="approaches-to-handle-load">Approaches to Handle Load</h3> <ol> <li><strong>Vertical scaling</strong>: Upgrade to a more powerful machine</li> <li><strong>Horizontal scaling</strong>: Distribute load across multiple machines <ul> <li><em>Elastic</em>: Automatically add resources when load increases</li> <li>Complex for stateful systems</li> </ul> </li> </ol> <p>Rule of thumb: Scale databases vertically until cost or availability requirements force distribution</p> <h2 id="maintainability">Maintainability</h2> <p>The majority of software cost is in ongoing maintenance, not initial development. Design for these three principles:</p> <ol> <li><strong>Operability</strong>: Make it easy for operations teams to keep the system running smoothly</li> <li><strong>Simplicity</strong>: Make it easy for new engineers to understand the system (reduce accidental complexity)</li> <li><strong>Evolvability</strong>: Make it easy to adapt the system for unanticipated use cases in the future</li> </ol>]]></content><author><name></name></author><category term="kleppman-data-systems"/><category term="data-systems"/><category term="reliability"/><category term="scalability"/><summary type="html"><![CDATA[The three fundamental concerns for building data systems: reliability, scalability, and maintainability]]></summary></entry></feed>