---
layout: post
title: "The Evolution of HTTP"
date: 2026-02-21 00:00:00
description: "HTTP/1.0 through HTTP/3 — what changed and why"
tags: http networking
categories: system-design networking
giscus_comments: false
related_posts: false
---

The **HyperText Transfer Protocol** (HTTP) is how browsers communicate to web servers.

- HTTP is an **application-layer** protocol (Layer 7) that runs on top of a transport protocol (**TCP**, or **QUIC** in HTTP/3)
- It follows a **request-response** model: A client sends a request, the server sends back a response.

HTTP has evolved significantly since inception. The goal of this blog post is to give a history of the evolution and describe the features and limitations of each version.

![alt text](/assets/img/http/image-9.png)



## HTTP/0.9 (1991) and HTTP/1.0 (1996)
![ccc text](/assets/img/http/image-4.png)


HTTP/0.9 was introduced by **Tim Berners-Lee** as part of the **World Wide Web**. It only supported `GET` requests and did not have headers or status codes (`200`, `404`, `500`, etc.). These were introduced in HTTP/1.0 along with `POST` and `HEAD`.

The original versions of HTTP did not support **persistent connections**. This meant that every request would require a new connection, which comes with additional overhead.

Each connection is initiated after a **TCP 3-way handshake**:
![alt text](/assets/img/http/image-5.png)

For **HTTP Secure** (HTTPS), there is an additional **TLS** (Transport Layer Security) handshake:
![alt text](/assets/img/http/image-7.png)

In HTTP/1.0, a connection was required for every resource. This means every image, CSS file, or JavaScript file required a new connection (and corresponding handshakes).

## HTTP/1.1 (1997)
HTTP/1.1 addressed many of the initial problems with HTTP/1.0, and is still widely used for simple websites.

### Improvements

- **Persistent Connections**: Instead of closing the connection after a single response, the server keeps the TCP socket open. This allows the client to send multiple requests through the same pipe without the overhead of repeated handshakes.

![alt text](/assets/img/http/image-11.png)




- **Chunked transfer encoding**: Servers could send results in smaller chunks. This made page rendering faster, since the server didn't have to wait for the whole response to be ready before sending chunks.

![alt text](/assets/img/http/image-14.png)

- **Pipelining**: Instead of waiting for a response before sending the next request, the client could send multiple requests at once.

![alt text](/assets/img/http/image-12.png)

### Head-of-Line Blocking

**Pipelining** failed in practice due to **head-of-line blocking**.

![alt text](/assets/img/http/image-16.png)

- The server must send responses in the same order that they were received.
- If the first result gets stuck, all subsequent requests are also blocked, even if they are ready to be sent.

Developers got around this using a few methods:
- **Domain sharding**: Serve static assets from subdomains (with separate connections).
- Make fewer requests by **bundling assets** together.

## HTTP/2 (2015)

<!-- Motivated by the limitations of HTTP/1.1 — too many connections, redundant headers, head-of-line blocking at the application layer.


### Improvements  -->
<!-- - **Binary framing** — requests and responses are split into binary frames instead of plain text

- **Multiplexing** — multiple streams (request/response pairs) interleaved over a single TCP connection, no application-level head-of-line blocking

- **Header compression (HPACK)** — compresses headers and avoids resending unchanged headers across requests

- **Stream prioritization** — client can hint which resources are more important

- **Server push** — server can proactively send resources it predicts the client will need

```
Client                          Server
  |                               |
  |===== TCP + TLS Handshake ====)|
  |                               |
  |  Single TCP Connection        |
  |  ┌──────────────────────────┐ |
  |  │ Stream 1: GET /page.html │─>
  |  │ Stream 2: GET /style.css │─>
  |  │ Stream 3: GET /image.png │─>
  |  └──────────────────────────┘ |
  |  ┌──────────────────────────┐ |
  <──│ Stream 3: PNG data       │ |  ← responses arrive in any order
  <──│ Stream 1: HTML data      │ |
  <──│ Stream 2: CSS data       │ |
  |  └──────────────────────────┘ |

  TCP packet loss:
  |  ┌──────────────────────────┐ |
  |  │ Stream 1: ████           │ |
  |  │ Stream 2: ██ X (lost)    │ |  ← TCP retransmit stalls
  |  │ Stream 3: ████           │ |     ALL streams
  |  └──────────────────────────┘ |
``` -->

<!-- ### Limitations: Transport-Level Head-of-line Blocking -->

## HTTP/3 (2022)

<!-- Motivated by TCP head-of-line blocking and slow connection setup.
### QUIC Transport
- **QUIC transport** — replaces TCP with QUIC, which runs over UDP
- **Independent streams** — a packet loss only affects the stream it belongs to, not all streams on the connection
- **Faster handshakes** — QUIC integrates TLS 1.3 into the transport, enabling 1-RTT or even 0-RTT connection setup (vs TCP's 2-3 round trips for TCP + TLS)
- **Connection migration** — connections are identified by a connection ID, not the IP/port tuple, so they survive network changes (e.g., switching from Wi-Fi to cellular)
 -->

<!-- ### Complexity -->


## Sources
Original HTTP Memos:
- [RFC 1945 — HTTP/1.0](https://datatracker.ietf.org/doc/html/rfc1945)
- [RFC 2616 — HTTP/1.1](https://datatracker.ietf.org/doc/html/rfc2616)
- [RFC 7540 — HTTP/2](https://datatracker.ietf.org/doc/html/rfc7540)
- [RFC 9114 — HTTP/3](https://datatracker.ietf.org/doc/html/rfc9114)

Additional Resources:
- [MDN — Evolution of HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Evolution_of_HTTP)
- [HTTP/1 to HTTP/2 to HTTP/3 (YouTube)](https://www.youtube.com/watch?v=UMwQjFzTQXw)
